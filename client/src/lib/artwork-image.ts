// Re-exports for legacy import paths. Prefer importing directly from
// @shared/responsive-image in new code (#573).
export {
  ARTWORK_SIZES,
  BLOG_SIZES,
  getResponsivePictureSources,
  getArtworkPictureSources,
  getArtworkTextureUrl,
  type ResponsivePictureSources,
  type ArtworkPictureSources,
} from "@shared/responsive-image";
