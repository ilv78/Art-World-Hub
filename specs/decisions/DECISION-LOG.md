# ArtVerse — Decision Log

**Status:** Active
**Last Updated:** 2026-09-17

Lightweight log for minor decisions that don't warrant a full ADR. For significant architectural decisions, create an ADR in `specs/architecture/ADR/`.

---

| Date | Decision | Context | Made By |
|------|----------|---------|---------|
| 2026-03-10 | Merge CI and deploy into single workflow (ci.yml) | Option A chosen over separate workflows — simpler to reason about | Architecture |
| 2026-03-10 | Skip branch protection (Step 4) | Requires GitHub Pro or public repo — **superseded by #100 on 2026-03-13** | Architecture |
| 2026-03-10 | Use single VPS for both staging and production | Separate Linux users on Webdock VPS, simpler than multi-server | Architecture |
| 2026-03-10 | Staging uses push mode, production uses migrations | Push mode is fast for disposable staging data; migrations are safe for production | Architecture |
| 2026-03-11 | Use Telegram for deploy notifications | Lightweight, personal — no Slack/Teams overhead for solo dev | Architecture |
| 2026-03-11 | Save rollback state as `.previous_image_tag` file | Simple file-based approach vs database/API — works with SSH emergency rollback | Architecture |
| 2026-03-12 | Add email/password auth alongside Google OIDC | Users without Google accounts need access; magic link signup ensures email ownership | Architecture |
| 2026-03-12 | Use Resend for transactional email | Simple API, good free tier, handles deliverability — replaced dead Replit connector | Architecture |
| 2026-03-12 | Restructure specs/ into taxonomy | Per DOC-AGENT-SPEC.md — enables automated documentation validation | Architecture |
| 2026-03-12 | Doc agent: Claude for PRs, shell script for audits | claude-code-action API doesn't support scheduled/push triggers well; shell script is reliable and free for structural checks | Architecture |
| 2026-03-13 | Pin all GitHub Actions to commit SHAs | Mutable tags (@v3, @v6) can be moved to malicious code — SHA pinning prevents supply chain attacks on CI/CD | Architecture |
| 2026-03-13 | Move `${{ }}` interpolation to `env:` blocks in workflows | Direct `${{ }}` in `run:` steps is vulnerable to shell injection if attacker controls the value (PR title, dispatch input) | Architecture |
| 2026-03-13 | Add ownership authorization to all write endpoints | Auth alone isn't enough — artist A could modify artist B's data. Ownership checks enforce resource-level access control | Architecture |
| 2026-03-13 | Add auth to MCP server endpoint | MCP exposed full CRUD with zero authentication — any HTTP client could manipulate all data | Architecture |
| 2026-03-13 | Block private IPs in image proxy (SSRF) | Image proxy could be used to scan internal network or access cloud metadata (169.254.169.254) | Architecture |
| 2026-03-13 | Validate file uploads via magic bytes, not just MIME header | Client-supplied MIME type is trivially spoofed — magic byte inspection verifies actual file content | Architecture |
| 2026-03-13 | HTML-escape user values in email templates | Unescaped user input (buyer name, address) could inject malicious HTML/phishing into notification emails | Architecture |
| 2026-03-13 | Use `DB_MIGRATION_MODE=migrate` in production | Push mode can destructively alter schema without review; migration mode requires explicit versioned SQL files committed to git | Architecture |
| 2026-03-13 | Security CI/CD pipeline with 7 scanning jobs (#55) | No security scanning existed; added gitleaks, npm audit, Semgrep, hadolint, Trivy, custom checks, and a gate job to cover repo, app, container, and supply chain | Architecture |
| 2026-03-13 | Release management: versioned image tags + smoke tests (#35) | Only `latest` Docker tag existed, making rollbacks impossible; added SHA + run-number tags, health checks, post-deploy smoke tests, and automated git release tagging | Architecture |
| 2026-03-13 | Enable branch protection on main (#100) | Direct push to main incident (#97) — technical enforcement needed since process alone was insufficient; requires PR, CI pass, enforced for admins | Architecture |
| 2026-03-13 | Pre-push hook via Husky to block pushes to main (#101) | Defense in depth after #97; decided NOT to run local tests on feature branches — CI + branch protection already cover that | Architecture |
| 2026-03-13 | Adopt blameless postmortem process (#98) | Based on Google SRE Book; created template, workflow spec, README index, and GitHub issue template in `docs/postmortems/` | Architecture |
| 2026-03-14 | RBAC with 3 roles: user, curator, admin (#8) | Platform had no access control beyond authentication; added `role` column, `isAdmin`/`isCurator` middleware, admin dashboard | Architecture |
| 2026-03-14 | Automated release workflow with label-based versioning (#110) | Releases were manual; `release.yml` auto-detects PATCH vs MINOR from issue labels (`feature`/`enhancement` = MINOR), generates CHANGELOG, creates git tag + GitHub Release | Architecture |
| 2026-03-15 | Structured JSON logging via pino, replacing console.log (#39) | No logging library existed; added child loggers per module, request correlation IDs, log rotation (14 days), persistent Docker volume, admin log-viewing API, and MCP `get_logs` tool | Architecture |
| 2026-03-15 | Users-artists cascade: auto-create artist on signup (#122) | Orphaned users/artists existed; cascade deletion + auto-create simplified the data model | Architecture |
| 2026-03-20 | Mobile: touch controls for 3D gallery, default to 2D view (#172) | 3D maze gallery was invisible on mobile; added drag-to-look, on-screen D-pad, swipe navigation; 2D as default on small screens | Architecture |
| 2026-03-23 | Upgrade Node.js from 20 to 25 (#73) | Foundation upgrade across Dockerfile, CI, and security workflows; prerequisite for other major dependency upgrades | Architecture |
| 2026-03-23 | Skip Docker build and staging deploy for docs-only changes (#206) | Every push to main triggered full pipeline even for markdown changes; added `dorny/paths-filter` to detect docs-only pushes | Architecture |
| 2026-03-24 | Migrate Tailwind CSS v3 → v4 (#196) | 2-5x faster builds, CSS-first config, smaller output; switched from PostCSS plugin to `@tailwindcss/vite`, removed autoprefixer/postcss deps | Architecture |
| 2026-03-24 | Migrate Zod v3 → v4 with drizzle-orm built-in integration (#198) | 6-14x faster parsing, 10x faster TS compilation, 57% smaller bundle; replaced deprecated `drizzle-zod` with `drizzle-orm/zod` | Architecture |
| 2026-03-24 | v3 redesign: parallel long-lived branch with preview environment (#234, #235) | Sidebar layout did not match art platform conventions; `redesign/v3` branch with dedicated preview environment allowed parallel development while main stayed stable | Architecture |
| 2026-03-25 | Auto-merge all passing PRs + auto-deploy on release tags (#249) | Manual merge of green PRs and manual production deploy after releases added unnecessary friction; auto-merge respects branch protection | Architecture |
| 2026-03-27 | Remove markdown issue tracker; GitHub Issues as single source of truth (#277) | Tracker duplicated GitHub Issues, generated noise in git history (one PR per closed issue), required a PAT secret, and went stale | Architecture |
| 2026-03-27 | Newsletter subscriber system with admin CSV export (#254) | v3 footer had signup placeholder; added `newsletter_subscribers` table, subscribe endpoint, admin management UI with selectable CSV export | Architecture |
| 2026-03-28 | Rebrand ArtVerse → Vernis9; keep infrastructure names unchanged (#244) | New brand from "vernissage" (gallery opening night); deliberately kept Docker, nginx, CI, localStorage, and cookie names to avoid breaking sessions and carts | Architecture |
| 2026-03-29 | Domain migration to vernis9.art (#288) | Rebrand required matching domain; configured DNS, nginx, SSL via certbot, redirect from old domain, updated health checks and notifications | Architecture |
| 2026-03-29 | v3.0 merge via structured 7-step process (#281) | Parallel branch strategy required careful merge: sync main → v3, QA on preview, merge PR, version bump, production deploy, post-deploy verify, domain migrate | Architecture |
| 2026-03-29 | CI validation: CHANGELOG must contain release version entry (#295) | v3.0.0 postmortem — CHANGELOG was updated after Docker image was built, serving stale content; CI now validates before release finalization | Architecture |
| 2026-03-29 | Deploy smoke test: verify /api/version matches deployed tag (#296) | Health check alone did not detect version mismatches; extended smoke tests to verify version + changelog content | Architecture |
| 2026-03-30 | Single orange accent (#F97316), remove 12-palette system (#345, #354) | 12-palette cookie-based system (~560 lines CSS) was over-engineered; one accent color provides consistent brand identity | Architecture |
| 2026-03-31 | SEO infrastructure: server-side meta injection, JSON-LD, sitemap, robots.txt (#364-369) | SPA had zero SEO — every route returned identical HTML; built complete infrastructure for crawlers, social previews, and structured data | Architecture |
| 2026-04-01 | Three-layer crawler blocking on non-production (#376) | Staging/preview publicly accessible; robots.txt + meta tag + X-Robots-Tag header prevent indexing of duplicate content | Architecture |
| 2026-04-02 | www → non-www nginx 301 redirect (#385) | Google Search Console reported canonical conflict between www and non-www; split nginx server block to redirect www to non-www | Architecture |
| 2026-04-02 | Sudoers-based nginx config management for SSH deploy users (#386) | staging/production users could manage Docker but not nginx; added `deploy-nginx-config` helper script with sudo, enabling SSH-based nginx deploys with automatic rollback on failure | Architecture |
| 2026-04-08 | Bump drizzle-orm 0.39.3 → 0.45.2 to clear GHSA-gpj5-g38j-94v9 (#417) | Security CVE for SQL identifier injection. Not exploitable in our codebase (no use of `sql.identifier`/`.dynamic()`), but blocks the CI security gate at `--audit-level=high`. Bump-and-run upgrade — migration journal version 7 unchanged, drizzle-kit 0.31.10 + drizzle-zod 0.8.3 already compatible, no API breaking changes affecting our surfaces | Security |
| 2026-04-08 | Doc-agent uses single rolling audit issue with dedup (#415) | Workflow was unconditionally creating a new GitHub issue on every push when findings existed → 11 duplicate `docs-audit` issues accumulated for the same `ST-004` warning. Replaced with state machine: find existing → comment OR create, close-all-on-clean. Fixed title `📋 Docs Audit (rolling)` so the issue list stays stable | Architecture |
| 2026-04-08 | Security workflow runs daily instead of weekly (#422) | GHSA-gpj5-g38j-94v9 sat undetected on `main` for ~28 hours during a quiet period — push-triggered scans only fire on PR activity, and the weekly Monday cron was 5 days away. Daily cron at 08:00 UTC closes the detection window. One-character cron change | Security |
| 2026-04-08 | Reverted: security cron stays weekly Monday (#425) — **superseded 2026-04-22 by #533** | The #422 issue framing implied no scheduled scan existed; the daily call was made on that premise before the correction. After re-reading, weekly Monday is acceptable: push-triggered scans still cover every PR, and a 7-day worst-case detection window for advisories against unchanged code is an acceptable tradeoff for less CI noise. Explicit risk acceptance | Security |
| 2026-04-09 | Trailing-slash canonicalization for vernis9.art (#427) | Google Search Console reported "Alternative page with proper canonical tag" for sitemap URLs. Two bugs: (a) homepage canonical was `https://vernis9.art` but URL is `https://vernis9.art/`; (b) `/foo` and `/foo/` both returned 200 with the same canonical. Fixed canonical to include trailing slash for `/`, and added nginx `rewrite ^/(.+)/$ /$1 permanent` to strip trailing slash from non-root paths | Architecture |
| 2026-04-09 | Staging nginx parity: apply trailing-slash redirect + add live config to repo (#431) | Staging was missing the trailing-slash rewrite applied to production in #427, and the live `staging.vernis9.art` nginx config (SSL + certbot) was not in the repo at all — only an obsolete pre-migration stub `staging.artverse.idata.ro.conf`. Added `deploy/nginx/staging.vernis9.art.conf` matching the live config with the rewrite, removed the stub. No SEO impact (staging is `noindex`), but establishes environment parity | Architecture |
| 2026-04-09 | Remove deprecated `baseUrl` from tsconfig to unblock TypeScript 6.0 upgrade (#436 / #400) | TS 6.0 promoted the `baseUrl` deprecation from warning to hard error. Our path aliases (`@/*`, `@shared/*`) work via the `paths` map alone — `baseUrl` was redundant. Rejected `ignoreDeprecations: "6.0"` which would just delay the work until TS 7.0. Clean removal preferred | Architecture |
| 2026-04-09 | Avoid `z.coerce.number()` with React Hook Form; use `valueAsNumber` at the input layer (#437 / #404) | Zod 4 changed `z.coerce.number()` input type from `number` to `unknown` to reflect what coercion does. `@hookform/resolvers@5` correctly propagates the split input/output types, which breaks `useForm<T>`'s single-type generic inference. Refactored `bidSchema` in `auctions.tsx` to use plain `z.number()` and coerce at the `<Input onChange>` level via `e.target.valueAsNumber`. Pattern to follow for future numeric form fields | Architecture |
| 2026-04-09 | Defer eslint-plugin-react-hooks v7 upgrade (#438 / #402) | v7 ships React Compiler rules that flag ~13 legitimate-but-imperative patterns in the Three.js 3D gallery (`hallway-gallery-3d.tsx`, `maze-gallery-3d.tsx`) — "cannot access refs during render", "cannot call impure function during render", "cannot modify value". Rejected (a) full adoption — multi-session refactor of stable gallery code is high-risk for a lint rule upgrade; rejected (b) targeted rule disable — gives false sense of v7 adoption without the benefit. Chose (c) defer: pin at v5.2.0, revisit when React Compiler is formally adopted or the 3D gallery is next touched for feature work | Architecture |
| 2026-04-09 | Multi-device dev workflow via WIP-commit handoff (#444) | Two-device dev (laptop `Dell` + a personal dev VPS) needs deterministic handoff for in-progress state; laptop is not reliably reachable inbound, so GitHub is the only practical sync channel. Three keywords: `park`, `resume`, `sync`. Spec at `specs/workflows/MULTI-DEVICE.md` | Architecture |
| 2026-04-09 | Clean up pre-migration nginx configs + add `remove-nginx-config` sudo helper (#434) | Three configs from before the vernis9.art domain migration: `artverse.idata.ro` works as a 301 to vernis9.art (KEEP — 5761 referer matches in prod log, preserves SEO link equity); `staging.artverse.idata.ro` was still actively serving the staging app on a non-canonical domain (REPLACE with 301 to staging.vernis9.art); `preview.artverse.idata.ro.conf` was dead, zero traffic (REMOVE). New `remove-nginx-config` helper added to mirror `deploy-nginx-config` (#386) so future symlink removals don't need root — shares the same flock to prevent races, backs up to `/etc/nginx/.removed/` for restore-on-failure | Architecture |
| 2026-04-10 | Spec corrections + redact dev-vps hostname from public docs (#447) | Two corrections discovered during #434 follow-up: (a) DEPLOYMENT.md was missing the actual sudoers filename (`nginx-deploy`); (b) MULTI-DEVICE.md (added in #445) wrongly claimed the personal dev VPS is on the same Webdock host as production — they're separate VPSs. Bundled with redaction of the dev-vps hostname from CLAUDE.md / MULTI-DEVICE.md / DECISION-LOG.md so the spec doesn't enumerate infrastructure attack surface beyond what public DNS already exposes. Acknowledged limitation: PR #444 / #445 git history retains the original text (impractical to rewrite history); the redaction reduces surface for future automated scrapers against current `main`, not historical leaks | Architecture |
| 2026-04-10 | Reconciled `/usr/local/bin/deploy-nginx-config` on VPS to match repo (#430) | The installed script lagged behind the repo by two unmerged improvements: sites-available + symlink convention (#386 follow-up) and ERR trap + flock (#389). Symlink reconciliation in `sites-enabled/` was already complete from manual / earlier cleanup — only the script binary needed updating. Installed via root, verified by md5 (`e13879d96f56cbffe53e541c6466ef41`), smoke-tested non-destructively with no-args usage error and nonexistent-source rejection (no live config touched). Future drift prevention via CI check is explicit follow-up territory | Architecture |
| 2026-04-10 | Production deploy stays manual — removed broken auto-trigger from `release-finalize.yml` (#443) | The `gh workflow run deploy-production.yml` step had been failing 403 on every release because `RELEASE_PAT` lacks the `actions:write` (fine-grained) / `workflow` (classic) scope. Considered three fixes: (a) rotate the PAT to add the missing scope, (b) convert `deploy-production.yml` to a reusable `workflow_call` workflow and call it from finalize using `GITHUB_TOKEN` (no PAT), (c) revert to manual. Chose (c): the developer prefers staging-verification-then-manual-promote — make a release, eyeball it on staging, then dispatch `deploy-production.yml`. Avoids both the PAT rotation and the latent race between `release-finalize` and `ci.yml`'s `build-image` job (auto-trigger with `image_tag=latest` could have deployed the previous `latest`). Auto-trigger was originally added 2026-03-25 for #249 — superseded today | Architecture |
| 2026-04-13 | Enable GitHub merge queue on `main` (#472) — prereq: `merge_group` trigger on ci.yml | Resolves the serial-rebase backlog documented in postmortem `2026-04-13-dependabot-auto-merge-backlog.md`. Branch protection's `required_status_checks.strict: true` made bursty Dependabot batches (10 PRs/week) un-drainable: each merge re-staled every other PR, and Dependabot rebases serially. Merge queue serializes rebase + check + merge automatically. Prerequisite delivered in this PR: `ci.yml` triggers on `merge_group` events so the required `Lint, Type Check, Test & Build` check runs against the queue's combined commit. Deploy/staging jobs gated on `event_name == 'push'` so merge_group will not trigger spurious deploys. **Note (2026-04-13 follow-up):** the merge-queue enable itself required migrating `main` from classic branch protection to a Repository Ruleset — see ADR-0009 for the why and the staged plan | Architecture |
| 2026-04-13 | Migrate `main` branch protection from classic to Repository Ruleset (#472) — see ADR-0009 | Required to enable GitHub merge queue: classic branch-protection UI/API does not expose merge queue for user-owned (non-org) repos. Stage 1 (ruleset created mirroring classic protection, both running in parallel) applied 2026-04-13. Source of truth for branch protection now lives in GitHub (ruleset id 14998880), not in the repo — ADR-0009 + this row are the in-repo record. Rollback: disable the ruleset (`enforcement: disabled`); classic protection is still active in parallel as a safety net until Stage 4. **Superseded same day — see next row** | Architecture |
| 2026-04-13 | Abandon merge-queue path; pivot to Dependabot grouping (#472 closed → #473) — supersedes ADR-0009 | Stage 2 of ADR-0009 (adding `merge_queue` rule) failed at every path tried: REST API rejected the rule type with no detail, classic branch-protection UI does not show "Require merge queue", rulesets UI does not allow adding new rules. Conclusion: GitHub does not expose merge queue for user-owned (non-org) repos. Cleanup: ruleset id 14998880 deleted (it only duplicated classic protection); ADR-0009 marked Superseded with a writeup of what was attempted; classic branch protection remains the sole protection on `main`; the `merge_group` trigger added in PR #477 is kept as a harmless no-op. New direction: action item #473 — broaden Dependabot grouping to make typical batch ~2–3 PRs/week, which the existing serial-rebase loop drains without a queue | Architecture |
| 2026-04-13 | Broaden Dependabot grouping; drop missing `dependencies` label (#473 closes #475) | npm and github-actions ecosystems now group both `minor` and `patch` updates into a single weekly PR per ecosystem (was: only `patch` grouped on npm; nothing grouped on github-actions). Major updates still get individual PRs (correctly held back by auto-merge policy). Docker ecosystem unchanged (rare updates, not worth grouping). Tradeoff accepted: one bad dep blocks the whole grouped PR — if this becomes painful, split by `dependency-type: production` vs `development` later. Bundled fix for #475: removed `labels: [dependencies]` from all three ecosystem entries since the label was never created and was generating noisy "label not found" comments on every PR. Author (`dependabot[bot]`) + commit prefix already make these PRs filterable | Architecture |
| 2026-04-14 | Upgrade React 18 → 19 (#485 closes #466, #482) | Investigation found zero blockers: codebase uses no removed APIs (no PropTypes, no defaultProps on functions, no string refs, no ReactDOM.render, no legacy Context — already on `createRoot`); all ~25 React-ecosystem deps (Radix UI x28, recharts, embla, react-hook-form, react-helmet-async, react-day-picker, wouter, vaul, cmdk, @tanstack/react-query, etc.) accept React 19 with zero peer-dep warnings. Automated checks (tsc, lint, tests, build) pass on React 19 with no source changes. Residual risk: runtime behavior changes React 19 introduced (Suspense boundary batch-reveal, stricter StrictMode double-render) — verified by manual smoke test before merge. Original #485 description estimated multi-day effort; investigation revised to ~1-hour single-PR operation | Architecture |
| 2026-04-15 | Split security-header ownership between nginx and helmet (#497) | Root cause of missing production headers was an outdated container (fixed by v3.7.0 deploy), not a config bug. As defense-in-depth, nginx is now authoritative for the four baseline headers (HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy) via `add_header ... always`, with matching `proxy_hide_header` in the proxying location to prevent upstream duplicates. Helmet remains authoritative for the application-shaped headers (CSP, COOP, CORP, Origin-Agent-Cluster, etc.) since those depend on app behavior. Referrer-Policy set to `strict-origin-when-cross-origin` (modern default, analytics-friendly) rather than helmet's `no-referrer`. Rationale for the split: if helmet regresses again (middleware ordering, version change, rollback to old image), the four baseline headers still reach clients from nginx; CSP can't live in nginx because it must evolve with frontend code. CSP itself tightened in the same PR — `style-src`/`font-src` narrowed from helmet's permissive `https:` to the specific Google Fonts origins (`fonts.googleapis.com`, `fonts.gstatic.com`) used by `client/index.html` | Architecture |
| 2026-04-17 | Public artwork URL scheme `/artworks/:slug` (#503) | Artworks needed crawlable public pages for SEO. Chose slug URLs over `/artworks/:id` to get readable, shareable URLs. Slug format `slugify(title)-<first-8-chars-of-uuid>` balances readability with uniqueness — the UUID suffix eliminates collision-detection code entirely, and mirrored SQL + TS slugifiers keep the backfill migration deterministic. Privacy gate is `isPublished=true` alone (not `isReadyForExhibition`): `isReadyForExhibition` only controls 3D-gallery inclusion; `isPublished` is the correct visibility boundary. Rejected: `/art/<id>`/`/works/<id>` (less descriptive), collision-detection retries on slug generation (complexity for nearly-impossible collisions at current scale). VisualArtwork JSON-LD + per-artwork OG image + sitemap entries ship in the same PR | Architecture |
| 2026-04-17 | `isPublished` gates `isForSale`/`isInGallery`/`isReadyForExhibition` on artworks (#513) | New draft/published state for artworks parallels the existing blog concept. Gating: a draft (`isPublished=false`) clamps the three commerce/gallery flags to false server-side, both on create and on unpublish-transition. `exhibitionOrder` is preserved across an unpublish so re-publishing restores the slot. Public GET endpoints filter drafts; owner sees own drafts via `/api/artists/:id/artworks` (passport.session populates `req.user` even on unauth'd routes, so owner identity is available). Migration backfills existing rows to `isPublished=true` to avoid silent disappearance on deploy. Rejected: 400-ing on flag conflicts — silent clamp is friendlier and the UI disables the gated switches when unpublished, so legitimate clients never hit a conflict. Dashboard surfaces drafts via a Published/Drafts tab split on both Portfolio and Blog sections | Data model |
| 2026-04-20 | Koningsdag landing at `/koningsdag` reuses `newsletter_subscribers` with a new `source` column (#526) | Koningsdag flea-market QR code needs an email-capture landing page. Chose to extend the existing `newsletter_subscribers` table with a `source varchar(50) default 'general' not null` instead of creating a separate `flea_market_leads` table — one admin UI, one endpoint, one unique-by-email index still holds. Sources are whitelisted on the route (`general`, `koningsdag-2026`) so the column can't be used as a free-text sink. Route `/koningsdag` opts out of `PublicLayout` (no top nav, footer, or bottom tabs) via a `BARE_ROUTES` check in `App.tsx` so the landing is a single-screen standalone per the design handoff. Page marked `noindex,nofollow` — campaign URL, not core SEO surface. Success state auto-redirects to `/` after 6s (user requested the page send visitors back to vernis9.art after submission) with a visible manual "Back to vernis9.art" link for users who don't want to wait. Resend confirmation-email send deferred — lead capture only for this PR | Data model |
| 2026-04-20 | Doc-agent rule `ST-004` ignores Dependabot-only commits when measuring workflow-file recency (#522) | `ST-004` (CI-CD.md staleness vs `.github/workflows/*.yml`) was re-firing every time Dependabot bumped a pinned Action SHA because the check used `git log -1 --format=%ct -- file` — raw mtime. After each weekly Dependabot bump, CI-CD.md was mechanically declared stale within a week even though the workflow's semantics were identical. Fix: added a `days_since_last_human_commit` helper (and a `file_has_changed_by_human_since` wrapper) in `.github/scripts/doc-audit.sh` that selects the most recent commit whose author email does not contain `dependabot[bot]`, falling back to the absolute latest commit if the file has only ever been touched by the bot. Wired into the ST-004 loop only — ST-005 (`DEPLOYMENT.md` vs `Dockerfile`) left on the original helper since it was not exhibiting the bug; the new helper is reusable if it starts to. Author-email match is narrow (`dependabot[bot]`) so co-authored-by humans and dependabot-created-then-edited-by-human commits still count as human. Same-PR cleanup: bumped CI-CD.md to document the `merge_group` trigger leftover from #477 | Architecture |
| 2026-04-22 | Suspend weekly security scan cron (#533) — supersedes #425 | Cadence under review; commented out the `schedule:` block in `.github/workflows/security.yml` so the Monday 08:00 UTC run stops firing. `push` and `pull_request` triggers remain active, so PRs are still gated by all security jobs (secret-scan, npm audit, Semgrep, hadolint, Trivy on `main`, custom-checks). Rejected: `gh workflow disable` (would also break PR gating), job-level `if:` conditions (unnecessary, trigger-level pause is minimal). A follow-up issue will re-enable the schedule with the new cadence once decided | Security |
| 2026-04-22 | Artist SEO Phase 1: Person `sameAs` + display-name data fix (#535) | Google cannot rank an artist profile for the artist's full name unless the literal string appears on the page and there's a trustworthy identity link (`sameAs`) to off-platform profiles. Two moves in one PR: (1) `server/meta.ts` now derives a `sameAs` array from the existing `artists.socialLinks` JSONB column, filtered to absolute `https?://` URLs so bare handles and relative paths never leak into structured data; (2) a one-off SQL update on each environment's DB to rename an artist row that had been stored as the short form `\"Alexandra C. \"` (with trailing space) to `\"Alexandra Constantin\"`. Rejected: (a) a Drizzle data-migration file for the rename — mixing data fixes with schema migrations sets a precedent we don't want, and staging uses `db:push` mode so migration files don't run there anyway; (b) adding a dedicated `displayName` column — zero current demand, premature abstraction. Data fix is documented in the PR description with the exact SQL for staging + production, or equivalently the artist dashboard UI path. Phases 2–4 (artist slug URLs, richer Person schema + lastmod, internal-link anchor text) tracked as follow-ups. | Architecture |
| 2026-04-23 | Artist SEO Phase 2: slug URLs `/artists/:slug` with UUID→slug + retired-slug 301 (#537) | Mirrors the artwork-slug pattern (#503): artist display names need to appear in the URL to give crawlers a keyword signal and give users a readable/shareable link. New `slug` column on `artists` (unique), backfilled via three-step migration identical in shape to `0008`. Chose a single lightweight `/api/public/artists/:slug` returning just `{ artist }` — the client then re-uses the existing id-keyed sub-queries for artworks / gallery / blog, which keeps React Query cache granularity intact and minimises client diff. Rejected a fat `{ artist, artworks, gallery, blog }` bundle (unbounded payload for heavy artists, bigger client rewrite). UUID → slug 301 redirect registered in `registerRoutes()` before the SPA catch-all so external links and any existing Google index entries flow link equity to the new canonical URL; nginx couldn't do the redirect because the slug lookup lives in Postgres. Slug regeneration scoped to rename only (not every update) to keep URLs stable under unrelated edits. **Rename durability:** added `artist_slug_history` table keyed on retired slugs with `ON DELETE CASCADE` back to `artists`. On rename, the old slug is inserted in the same transaction as the `artists.slug` update; the route handler 301s retired slugs to the current slug. Rejected a `previous_slugs text[]` column on `artists` (GIN index overhead, no per-retirement timestamp, awkward lookup). Rejected "accept the breakage" — a rename shouldn't break accumulated link equity, and the history-table cost is 24 bytes per rename. | Architecture |
| 2026-04-23 | Migrate Trivy ignores from classic `.trivyignore` to v2 `.trivyignore.yaml`; path-scope drizzle-kit esbuild CVEs (#540) | Two new Go stdlib CVEs (CVE-2026-32281, CVE-2026-32283) surfaced in drizzle-kit's nested `esbuild@0.25.12` binary (Go 1.23.12) after PR #534 merged, failing both Trivy jobs on `main` and skipping `Deploy to Staging` for two consecutive merges (#534, #536). Root cause is not remediable from our side: drizzle-kit pins `esbuild@^0.25.x`, the fix requires esbuild >= 0.26 (built on Go >= 1.25.9), so we cannot bump our way out. Chose path-scoped yaml suppression over: (a) `npm overrides` forcing drizzle-kit onto esbuild 0.28.x — 0.25 → 0.28 spans multiple esbuild breaking minors, risk/reward too high for a CI unblock; (b) multi-stage Docker split to drop drizzle-kit from the prod image — deferred as a hardening task. Trivy's `--ignorefile` accepts only one file and can't mix classic + yaml formats, so the classic `.trivyignore` was removed and every pre-existing global entry was ported to yaml (no `paths` field = same global behavior). The two new entries are path-scoped to the specific drizzle-kit binary so unrelated future CVEs on the same IDs would still fail CI. Every entry carries a `statement` + `expired_at: 2026-10-23` to force a six-month re-review. | Security |
| 2026-05-01 | Compress text responses at nginx, not in Express's static middleware (#550) | Lighthouse 12 mobile audit on staging (post-#549) flagged `uses-text-compression` with claimed savings of 7000ms LCP / 6650ms FCP. `curl -I` showed Express's `compression` middleware was gzipping HTML through the proxy but the bundled `/assets/*` files (1.76 MB JS, 140 KB CSS) were passing through uncompressed. Chose nginx-layer compression (`gzip on; gzip_proxied any;` in both `vernis9.art.conf` and `staging.vernis9.art.conf`) over fixing Express's middleware ordering: nginx is already the security-headers source of truth (#497), the HTTP-encoding policy is symmetrical, nginx auto-skips already-compressed upstream responses so HTML is not double-handled, and the change is one config block per host with auto-rollback via `deploy-nginx-config`. `gzip_types` includes both `application/javascript` (legacy) and `text/javascript` (RFC 9239 / what Express's static middleware actually serves) — the first attempt missed the latter and only CSS got compressed until the type was added. Brotli not configured: the Ubuntu system `nginx 1.24.0` package is not built with `ngx_brotli`, and rebuilding nginx for one extra encoding is not worth the maintenance cost; gzip already produces a 73% reduction on the JS bundle (1.76 MB → 471 KB). Out of scope but flagged in the same audit for #551: enable HTTP/2 and fix `Cache-Control: max-age=0` on hashed `/assets/*`. | Infrastructure |
| 2026-05-01 | Enable HTTP/2 + path-scoped Cache-Control immutable for `/assets/*` and 30d for `/uploads/*` (#555) | Lighthouse 12 audit (post-#552) flagged `uses-http2` (~450ms LCP) and `modern-http-insight` (~900ms LCP) — both pages were on HTTP/1.1 — and `curl -I` showed `Cache-Control: public, max-age=0` on the Vite content-hashed `/assets/*` bundles, forcing repeat visitors to redownload the (now-gzipped) 471 KB JS bundle on every page load. Combined fix in two new `location` blocks per host plus the `http2` directive on the HTTPS `listen` lines. **Cache strategy split between paths**: `/assets/*` → `public, max-age=31536000, immutable` (Vite content-hashes the filename, so a code change generates a new path; safe to lock for 1 year), `/uploads/*` → `public, max-age=2592000` (30 days, **not** immutable) because the URL is UUID-keyed but the same UUID could in principle be re-uploaded from the admin UI; 30d is a conservative future-proofing window vs. the 1y immutable that would prevent invalidation. Rejected: regex `location ~* "^/(assets\|uploads)/"` (more compact but harder to evolve when `/uploads/*` migrates to S3); single nginx-wide cache rule (the two paths have different invalidation semantics). **nginx `add_header` inheritance gotcha**: defining `add_header` inside a `location` block cancels the parent server's `add_header` inheritance, so the four baseline security headers (HSTS / X-Frame / X-CTO / Referrer-Policy) had to be repeated inside each new block — verbose but explicit. **Known cosmetic warning**: `protocol options redefined for 0.0.0.0:443` fires once at `nginx -t` because the alphabetically-first sites-enabled config (`artverse.idata.ro`) declares `listen 443 ssl;` (no http2), making our later `listen 443 ssl http2;` a redefinition. Cross-site cleanup tracked separately; the warning is benign — h2 is verifiably active on all vernis9.art hosts. | Infrastructure |
| 2026-05-02 | Generate responsive WebP/JPEG variants on artwork upload, keep originals (#564) | Lighthouse 12 mobile audit post-v3.13.0 deploy (2026-05-02) flagged `uses-responsive-images` (8 MB / 2.65s LCP claimed savings) and `modern-image-formats` (~2 MB additional) on `https://vernis9.art/` — artwork uploads were stored at full upload resolution and served as-is. New `server/lib/artwork-image.ts` generates 4 WebP widths (480/960/1440/2400) + 2 JPEG fallbacks (960/2400) on every artwork upload via existing `sharp@0.34.5` (avatar pipeline pattern from #359). **Always-write-all-six policy**: every variant file must exist for the `<picture>` srcset URLs to resolve — when source is smaller than a target width, the variant is written at source size (sharp `withoutEnlargement`) rather than skipped; the alternative (skip + missing files) was tried first and broke the SPA-fallback path because Vite serves `index.html` for any unknown URL, so the browser received HTML where it expected WebP and `naturalWidth` came back 0. **Original retained**: full-resolution upload kept alongside the variants (~2× disk on `/uploads/artworks/`) to preserve a source of truth for future re-processing (AVIF, new sizes, quality tweaks). Rejected: delete-after-process (locks variant set); LQIP/blurhash placeholders (premature for current page-load profile); on-the-fly generation middleware (latency on first request, complexity tax forever). Decision flagged for revisit 2026-11-02 via scheduled agent. **`<picture>` layout**: `display: contents` inline style on the `<picture>` element so it doesn't introduce its own layout box — the inner `<img>`'s sizing classes (`w-full h-full object-cover` etc.) keep working as if the picture wrapper weren't there. Without this, `<picture>` defaults to `display: inline` and `<img>`'s `w-full` collapses to zero against an inline parent. **Out of scope**: 3D galleries (Three.js textures still load original; follow-up issue), admin dashboards (low traffic), AVIF (defer until WebP win is measured), `<img>` width/height attrs for CLS (orthogonal, see #565). | Infrastructure |

| 2026-05-03 | Self-tracked share analytics with append-only `share_events` table; no public per-platform counts (#569) | Per-platform public share counts have been deprecated/removed by Facebook (2018), LinkedIn (2017), and never existed for X / Bluesky / Instagram — Pinterest's count API is the lone survivor and unreliable. Showing "0" or "—" on five of six platforms degrades the UI, so per-platform counts dropped. Self-tracked clicks chosen instead: a new `share_events` table (varchar pointers to four item types — no FK, append-only, decoupled from cascade-delete) populated by `POST /api/share-events` (Zod-validated, **6/min/IP** rate limit per @ilv78 — much tighter than the global 100/15min because a normal user shares a handful of times in a session at most), aggregated by `getShareEventStats(sinceDays)` for an admin Shares tab. **No site-wide analytics tool** (Plausible/PostHog/etc.) was adopted in this work — that's a larger decision involving privacy banners and tool choice; deferred to a separate issue. **UTM tagging** added on every outbound share URL (`utm_source=<platform>&utm_medium=share&utm_campaign=<itemType>`) with incoming `utm_*` stripped first to prevent duplicates when a user re-shares an already-tagged URL. **Instagram desktop button intentionally omitted** — IG has no public web share intent; on mobile the OS share sheet via `navigator.share` covers it. **Three indexes** on `share_events`: `(item_type, item_id)`, `created_at`, `platform` — sized for the admin aggregations rather than per-row reads. | Architecture |

| 2026-05-04 | Branded OG cards rendered server-side via sharp + SVG, brand fonts bundled in repo (#577) | Item-share OG previews on Facebook/LinkedIn/X were the bare source asset (no Vernis9 brand context). Pure CSS overlays do not work for OG — social crawlers fetch the raw `og:image`. Two server-side options were considered: (a) sharp + SVG composite (what we ship — `sharp@0.34.5` already pulled in by the artwork variant pipeline #564, no new native dep) vs (b) `node-canvas` (heavier native dep, better text rendering). Picked (a) because the marginal text-quality difference is invisible at 64px display weight and avoiding a second native build is worth more than perfect kerning. **Fonts bundled in repo (`assets/fonts/`)** rather than apt-installed (option 2) or `@resvg/resvg-js` (option 3) — bundled TTFs give exact brand fidelity (Playfair Display Bold + Inter Regular, SIL OFL, ~440KB total) and survive any base-image swap; the alternative `apt install fonts-liberation` has no Playfair-equivalent. Dockerfile gains an `apt install fontconfig` + `COPY ... /usr/share/fonts/truetype/vernis9 + fc-cache -f` block. **Cache invalidation by source-image mtime** rather than DB `updatedAt` because `artworks` and `artists` tables have no `updatedAt` column (would need a migration); cache-busting `?v=<8charHash>` on the OG URL handles FB external cache. **Static routes intentionally untouched** — only item shares get the branded card per @ilv78 instruction; `/`, `/store`, `/gallery` etc. keep the existing `og-default.png`. | Architecture |

| 2026-05-11 | Move branded OG card endpoint out of `/api/*` (#593) | Facebook's scraper silently skips og:image URLs whose path starts with `/api/` — confirmed via full-history grep of rotated nginx access logs: zero FB hits to `/api/og/...` despite hundreds of FB scrapes of the parent HTML pages whose og:image meta points there. Other scrapers (WhatsApp, LinkedIn, Telegram, iOS) fetched the original path fine. Considered but rejected: nginx-level alias serving `/og/...` while keeping the route at `/api/og/...` (too clever, two URLs for one resource, no backward-compat upside since OG URLs aren't bookmarked); a `?path=` shaped escape hatch (still under `/api/`, same heuristic likely fires). Picked the simplest fix — move the route. On-disk cache at `uploads/og-cards/<type>-<id>.jpg` unchanged; only public route prefix moves. No redirect from the old path: external caches (FB/X) repopulate on next scrape, no in-app feature bookmarks OG-card URLs. | Architecture |
| 2026-05-11 | Remove unused `ui/calendar.tsx` + drop `react-day-picker` dep (#606) | Dependabot opened #603 for a v9→v10 major bump. Investigation showed the Shadcn `Calendar` wrapper is imported nowhere in the app (template scaffold left over from initial setup; the 6 in-app `Calendar` references are the lucide-react icon, a different module). Removing the wrapper + dropping the dep is cleaner than maintaining a v10 upgrade for code we don't use; if we ever need a date picker we install the latest fresh. Closed #603 in favor of this. | Architecture |
| 2026-05-11 | Custom nginx `detailed` log_format + edge-level probe blocking with `return 444` (#548) | Default `combined` format omitted `$request_time`, `$upstream_response_time`, `$body_bytes_sent`, `$upstream_cache_status`, `$gzip_ratio` — no way to compute p50/p95/p99 latency or distinguish slow upstream from slow client. New format defined in `/etc/nginx/conf.d/log-format.conf` (source `deploy/nginx/log-format.conf`) so it lives in `http {}` context as required by the directive, with each site writing to a **dedicated** `/var/log/nginx/<host>.access.log` rather than overwriting the global file — keeps combined-format history clean and avoids mid-rotation format mixing. The conf.d install is a one-time root step outside the passwordless sudo allowlist (which only covers `deploy-nginx-config`, `nginx -t`, `nginx -s reload`); subsequent per-site edits deploy normally. Probe paths blocked at the nginx edge (`/.env`, `/.git`, `/.aws`, `/.boto`, `/.svn`, `/.hg`, `wp-admin`, `wp-login.php`, `wp-includes`, `xmlrpc.php`, `phpinfo.php`, `phpmyadmin`) with `return 444` — TCP connection drop, no HTTP response, no upstream fan-out. Chosen over a shared `include` snippet to keep deploys as single-file `scp` per site, matching the rest of the config layout. fail2ban-style IP banning for 404/401 bursts was deliberately deferred to a follow-up issue: it's a system-level install (apt + jail config + filter rule + log path coupling) with materially different scope. | Architecture |
| 2026-05-09 | Use `npm overrides` to coerce MCP-SDK transitive deps onto patched versions; bump `express-rate-limit` to `^8.5.1` (#595) | Five new GHSA advisories (1 high `fast-uri`, 4 moderate `hono` / `@hono/node-server` / `ip-address`) blocked all PR merges via the security gate starting 2026-05-09. Three of the five vulns sit deep inside `@modelcontextprotocol/sdk@1.29.0` (latest published; no upstream release available to consume), so direct dep bumps cannot reach them. `npm overrides` for `fast-uri@^3.1.2`, `hono@^4.12.18`, `@hono/node-server@^1.19.13` was chosen over (a) waiting for an MCP SDK release of unknown ETA — would block all merges indefinitely — and (b) forking MCP SDK — much heavier maintenance burden for a transitive issue. The fifth vuln (`ip-address` via `express-rate-limit`) resolves naturally with the patch bump `8.4.1 → 8.5.1`. All bumps stay within the SemVer-major boundary; `@hono/node-server` deliberately stays on `1.x` (latest is `2.x`) to avoid an MCP-incompatible major bump. Overrides are reviewed each time MCP SDK ships a new version — once SDK upstreams the fixes, the override entries get removed. | Architecture |
| 2026-05-04 | Relax helmet `Cross-Origin-Resource-Policy` default from `same-origin` to `cross-origin` (#590) | Helmet defaults block social embedders (Facebook OG, Telegram, LinkedIn, X) from rendering preview cards that reference any image we serve from `/og-default.png`, `/uploads/*`, favicons, etc. The branded OG-card endpoint #577 worked around it per-response, but static-route shares (`/`, `/store`, `/gallery`, etc.) still failed because their og:image is the static `og-default.png`. Vernis9 is an art-marketplace whose entire value proposition is content being shareable and embeddable across the web — `cross-origin` is genuinely the right default for this app. The `same-origin` policy primarily defends against Spectre-style side-channel reads of cross-origin JSON via `<script src="...json">`; sensitive APIs (admin CRUD, mutations) are auth-protected anyway, and the public read APIs (`/api/artworks`, `/api/artists`, `/api/blog`) are intentionally cross-origin-readable for SEO crawlers and third-party integrations. Considered but rejected: per-static-handler middleware overrides — fragile (Vite dev middlewares bypass it; new asset paths drift out of sync) and obscures the actual security posture. The og-cards route keeps its explicit per-response CORP override as defense in depth + self-documentation. | Architecture |
| 2026-05-26 | Move Trivy to PR-side + add main-CI failure notifier (#631) | The v3.18.0 release surfaced a real gap: Trivy was gated to `refs/heads/main` in both `ci.yml::scan-image` and `security.yml::container-scan`, so auto-merge fired before either ran. Six consecutive main-CI pushes failed silently on Trivy between 2026-05-18 and 2026-05-25 — staging was frozen on the v3.17.1 image for 12 days while PRs kept stacking on `main`. Compounding it, the Telegram notification lived inside `deploy-staging` (`if: always()`), but that step never executes because skipped jobs don't run their steps. **Fix A:** merged `build-image` + `scan-image` into a single `build-and-scan` job that runs on PR + push, with `load: true` so the image is available locally and `push: true` gated on `main`/`redesign/v3` push — Trivy now runs on PR-side and is naturally a required check for auto-merge. **Fix B:** new `ci-failure-notify.yml` triggered on `workflow_run` completion of CI/CD or Security workflows when `conclusion == failure` AND `head_branch == 'main'`, pings Telegram with workflow name, SHA, and run URL. **Rejected:** (a) GitHub Merge Queue — `merge_group:` trigger is already wired into `ci.yml` but enforcement requires branch protection which needs GitHub Pro on private repos (memory item from initial CI/CD setup); also overkill at solo-dev scale where there's no concurrent-merge problem to linearize. (b) Pinning the Trivy CVE DB version — would close the temporal-drift window completely but adds a maintenance burden of manually refreshing the pin; Fix B (loud failure on main) is the lazier-but-effective tradeoff. (c) Deduping the two redundant Trivy jobs (`ci.yml::build-and-scan` and `security.yml::container-scan`) — pre-existing redundancy, orthogonal to this fix, deferred. | Architecture |
| 2026-05-31 | Suppress 3 new drizzle-kit esbuild Go-stdlib HIGH CVEs in `.trivyignore.yaml` (#635) | CVE-2026-39823/39825/39826 disclosed since 2026-05-25 blocked all PRs (Trivy is PR-side since #631). Same non-reachable drizzle-kit-bundled-esbuild class as #540/#612 — build-time bundler, no network surface, fix blocked on drizzle-kit bumping esbuild >= 0.26. Path-scoped, `expired_at: 2026-10-23`. Fixed off `main` so both `main` and the new `v4` line unblock | Security |
| 2026-06-14 | Suppress esbuild GHSA-gv7w-rqvm-qjhr (global) + drizzle-kit esbuild CVE-2026-42504 (path-scoped) in `.trivyignore.yaml` (#649) | Two new HIGH advisories disclosed into the Trivy DB after 2026-05-31 blocked all PRs / staging deploys (Trivy is PR-side since #631). `GHSA-gv7w-rqvm-qjhr` is a Deno-specific esbuild install-integrity bypass flagged on every esbuild copy in the image (<0.28.1: top-level 0.28.0, drizzle-kit 0.25.12, esm-loader/tsx 0.27.4) — globally suppressed because it genuinely affects all copies and the image ships no Deno runtime (esbuild runs at build time via Node, so the vulnerable path never executes); revisit when esbuild >= 0.28.1 lands across the tree. `CVE-2026-42504` is a Go-stdlib MIME-header DoS in the drizzle-kit-bundled esbuild Go binary — same non-reachable build-time-bundler class as #540/#612/#635, path-scoped, fix blocked on drizzle-kit bumping esbuild >= 0.26. Both `expired_at: 2026-10-23`. | Security |
| 2026-07-05 | "Price on request" via a dedicated `priceOnRequest` boolean + nullable `price`, not a free-text price field (#668) | Artists need to publish works as available/price-on-request (most works are POR for this gallery). The literal request — free-text in the price field — was rejected: `artworks.price` is a numeric `decimal(10,2)` feeding cart totals, checkout, orders, auctions, price-sort and `formatPrice()` (which `parseInt`s → free text renders "NaN €"); overloading it with text would break all of those. Chosen: add `price_on_request` bool (DB default **false** so existing priced works are untouched; the edit form defaults the toggle **on** for new works) and drop NOT NULL on `price`. A single `formatArtworkPrice()` client helper renders "Price on request", POR works swap Add-to-Cart for an **Enquire** modal form (name/email/message → `POST /api/artworks/:id/enquire` → Resend email to the artist, reply-to the visitor, site-inbox fallback, rate-limited; not persisted) — chosen over a raw `mailto:` for a smoother UX that doesn't depend on the visitor's mail client — and order endpoints reject POR server-side. Zod `.refine` requires a price only when POR is off. Keeps the numeric column safe everywhere and cleanly separates from `isForSale=false` ("not for sale at all"). | Architecture |
| 2026-07-05 | Clear accumulated security backlog to green `main` after idle period (#670) | `main` CI had been red since 2026-06-22 (advisory-DB drift while the repo sat idle), blocking staging auto-deploy across three gates — none caused by app code. **npm audit:** lockfile refresh only (no `package.json` range changes; all fixes in-range) → hono 4.12.27, multer 2.2.0, qs 6.15.3, form-data 4.0.6, brace-expansion patched; `--audit-level=high` exits 0. **Trivy:** the same hono/multer bumps clear the library CVEs; the two non-fixable-by-us findings are suppressed with expiry — CVE-2026-27145 (Go crypto/x509 in drizzle-kit's build-time esbuild Go binary, recurring #540/#635/#649 class) and CVE-2026-48815 (sigstore bundled in the npm CLI base image, absent from our lockfile, same class as the cross-spawn/glob/tar entries). **Semgrep:** three supply-chain-hardening rules new to the registry packs (`github-actions-mutable-action-tag`, `dependabot-missing-cooldown`, `npm-missing-minimum-release-age`) `--exclude-rule`'d as out-of-scope for the OWASP app-security scan; genuine hardening deferred to #671 (SHA-pin actions, dependabot cooldowns, npm min-release-age). Chosen over broader remediation to unblock deploys tonight without a large, risky action-pinning sweep. | Security |
| 2026-07-10 | Enforce per-tool authorization on the MCP endpoint via session-bound user identity (#681) | Security review found broken function-level authorization across all 12 MCP tools + the `orders-by-artist` resource: `/mcp` was gated only by `isAuthenticated`, and self-registration is open, so any logged-in user could mutate any artist's artworks/profile/blog/orders, read buyer PII, and dump application logs (`get_logs` had no admin check, unlike `/api/admin/logs`). Chosen: bind each MCP session to the initializing user (`createMcpServer(userId)`, `userId` stored in the sessions map, cross-user session reuse → 403) and replicate the exact REST ownership checks (`storage.getArtistByUserId` + owner comparison; `authStorage.getUser().role === "admin"` for logs) inside each tool/resource/prompt handler. Rejected: (a) restricting `/mcp` to admins only — kills the artist-facing MCP use case the feature exists for; (b) a shared middleware layer — MCP tool handlers don't see the Express request, so checks must live at the handler level with identity injected at server construction. Buyer-side tools (`place_bid`, `create_order`) and public reads stay open to all authenticated users, matching their public REST equivalents. Cross-tenant denial + session-binding covered by `server/__tests__/mcp-auth.test.ts`. | Security |
| 2026-09-10 | Cross-VPS pull backups to the dev VPS + pre-migration dump in the production deploy (#682) | The platform had **no backups of any kind** — `deploy/`, all workflows and `docker-entrypoint.sh` contained zero `pg_dump`; a lost `pgdata` volume, a compose project mixup or a destructive migration would have permanently lost every order, user, artist and artwork, and the rollback workflows restore only the app image. Two parts. **(a) Pre-migration dump:** production applies migrations on container start via `docker-entrypoint.sh` + `DB_MIGRATION_MODE=migrate`, so the dump must run before `docker compose up -d` — the explicit `drizzle-kit migrate` later in the deploy script is a no-op second pass, and a dump placed there would already be post-migration. Fails the deploy on error (no `|| echo` masking, cf. #543/#692) and keeps the last 10. **(b) Scheduled backups:** cron on the **dev VPS** (`ilc01node03`) pulls DB dumps every 12h and the `uploads` volume nightly from production/staging (`ilc01node02`) over the existing `artverse-deploy` SSH key. **Pull over push** so production holds no credentials for the backup host and cannot delete its own history if compromised. **Chosen over Backblaze B2 / rclone** (the original plan) at the developer's direction — no third-party account or card needed, and the two boxes are genuinely separate nodes; the accepted trade-off is that a Webdock account-level failure takes out both, so the script keeps an unset `BACKUP_REMOTE` rclone hook to add an off-provider copy as a one-line change. **Rejected:** Webdock whole-box snapshots (same provider, no per-DB restore granularity) and a scheduled GitHub Actions job SSH-ing in (couples backups to GitHub availability, routes production data through hosted runners). Uploads are stored as `rsync` mirror + `cp -al` hardlink snapshots rather than dated tarballs — 30 snapshots cost ~138 MB instead of ~4 GB. Every dump is parsed back with `pg_restore --list` before being kept, because a dropped SSH stream otherwise yields a truncated backup that only fails at restore time. RPO 12h; WAL/PITR explicitly out of scope. Restore drill executed 2026-09-10 and logged in DEPLOYMENT.md §9. | Architecture |
| 2026-09-10 | Clear npm audit backlog by bumping ranges + two new `overrides` (#722) | `npm audit` had been red on `main` for weeks (14 vulns: 5 high, 7 moderate, 2 low), taking the `Gate` check down and blocking every PR — the sibling of #710 on the container-scan side. **`npm audit fix` was a no-op**: npm reported "fix available" on all 14 but resolved to `up to date`, because the lockfile already held the newest versions the declared ranges allowed. Remediation therefore had to move the ranges. Direct deps: `multer ^2.1.1 → ^2.3.0` (4 DoS advisories + a file-size-limit bypass race) and `sharp ^0.35.3 → ^0.35.4` (bundled libheif CVEs — verified the installed build reports libheif 1.23.2). Existing overrides refreshed: `hono → ^4.13.5`, `fast-uri → ^3.1.6` (staying on 3.x; the 4.x major is not required since 3.1.7 is patched), `@hono/node-server → ^1.19.15` (deliberately still 1.x — 2.x remains MCP-incompatible per the 2026-05-09 entry). **Two overrides added** because the transitives would not resolve from a lockfile refresh alone: `ip-address ^10.7.0` (pulled by `express-rate-limit@8.6.2`; three SSRF / trust-boundary-bypass advisories, and this package parses client IPs for rate limiting, so it is genuinely reachable) and `brace-expansion ^5.0.9` (pulled by `eslint → minimatch`; dev-only and not shipped in the image, but the CI gate does not distinguish dev from prod). Result: 14 → 7 findings, **0 high**, so `npm audit --audit-level=high` exits 0. The remaining 5 moderate + 2 low sit below the gate and are left rather than force-bumped. Rejected: `npm audit fix --force` (pulls unreviewed majors, including `@hono/node-server` 2.x) and `.trivyignore`-style suppression (real, reachable advisories with published fixes — suppression is for the non-reachable build-time-bundler class, cf. #540/#635/#649). Verified: 225 tests pass, `tsc` clean, and the real `sharp` pipeline exercised directly — 6 artwork variants + OG-card compositing + JPEG/PNG/WebP/AVIF round-trips. | Security |
| 2026-09-10 | Fix `libgnutls30` in the Dockerfile rather than re-suppressing it; suppress the npm-bundled + Go-stdlib remainder (#710) | Trivy had blocked `main` since 2026-07-13 — staging served a 2026-07-10 build for ~2 months. Fresh scan: 21 findings (3 CRITICAL, 18 HIGH) in three groups. **(a) `libgnutls30` (2 CRITICAL + 3 HIGH):** the five entries suppressing these had `expired_at: 2026-07-25` and had lapsed, which is part of why the gate went red. They pointed at #629 "base-image rebuild" as the fix — but that does not work: as of 2026-09-10 the current `node:25-bookworm-slim` still ships `3.7.9-2+deb12u6` while bookworm has `3.7.9-2+deb12u7` in the repo. Chosen: `apt-get install -y --only-upgrade libgnutls30` appended to the existing `fontconfig` layer in the run stage — a real fix, no new layer, self-cancelling once upstream rebuilds — and the five suppressions **deleted** rather than date-extended. Verified: the built image reports `3.7.9-2+deb12u7`. **(b) npm-CLI-bundled (1 CRITICAL + 7 HIGH):** `tar`, `pacote`, `brace-expansion`, `ip-address` — suppressed as the same class as the existing cross-spawn/glob/minimatch entries. This corrects the issue's proposed step 2 ("lockfile refresh / overrides" for these): verified against the tree that `tar` and `pacote` are **not in our dependency tree at all**, and #722 had already moved our own `brace-expansion` to 5.0.9 and `ip-address` to 10.7.0 while Trivy still reported 5.0.4 / 10.1.0 — proving the flagged copies live in the base image's bundled npm, where no lockfile change can reach them. **(c) Go stdlib in drizzle-kit's esbuild (8 HIGH):** fifth recurrence of the #540/#612/#635/#649/#670 class; path-scoped, build-time-only binary with no runtime network surface. All new entries `expired_at: 2026-10-23`, matching the existing batch so the whole file reviews in one pass. Verified locally with the exact CI flags (`--severity CRITICAL,HIGH --ignore-unfixed --exit-code 1`): exit 0. | Security |
| 2026-09-11 | Split the container scan into a blocking gate and a vendored-content warning (#728) | Trivy failed the build on any CRITICAL/HIGH anywhere in the image, drawing no distinction between dependencies we can upgrade and software vendored inside things we cannot version. Because the advisory DB updates continuously and independently of us, every upstream disclosure against vendored content became a red `main` whose only remedy was hand-writing a suppression — the structural cause behind #631, #670 and #710 (the last blocked staging for 61 days; 16 of its 21 findings were vendored). Each of those was closed by fixing the specific CVEs, so the mechanism survived all three. **Chosen:** `.github/trivy-vendored-paths.json` declares path globs for content we cannot version (`usr/local/lib/node_modules/npm/**` — the npm CLI bundled in the base image; `**/node_modules/drizzle-kit/node_modules/@esbuild/**` — drizzle-kit's build-time esbuild Go binary), each with a `reason`. Trivy now runs with `exit-code: 0` and `format: json`; `script/partition-trivy.mjs` matches each finding's `PkgPath` (falling back to `Target`), renders two tables to the job summary, and exits non-zero **only** on blocking findings. Applied to both scan jobs (`ci.yml::build-and-scan`, which gates staging deploy, and `security.yml::container-scan`). **OS packages always block** regardless of path — they ship with the base image but a Dockerfile `--only-upgrade` still fixes them, which is exactly how #710 fixed `libgnutls30`; treating them as vendored would have masked a real, fixable CRITICAL. The partitioner is plain `.mjs` with no imports beyond node builtins, because both scan jobs build a Docker image and never `npm ci` — a `.ts` file would have needed a `tsx` install in the scan path. **Verified** against a locally built production image with `.trivyignore.yaml` not applied at all: 0 blocking, 32 vendored (10 npm-bundled, 22 drizzle-kit esbuild), no misclassification — i.e. the two path patterns subsume all ~45 hand-written suppressions. **Deliberately not done:** pruning those suppressions. `.trivyignore.yaml` is untouched here so that a partitioner bug cannot silently unsuppress 45 CVEs with nothing left to catch them; the prune follows once CI has been observed classifying the same findings correctly. | Security |
| 2026-09-11 | Close #727 without building escalation; record the pre-#695 `/mcp` exposure as not exercised (#732) | Two postmortem action items settled together. **(a) #727 — CI alert escalation, closed as accepted.** The postmortem recorded that the red-`main` alert fired ≥13 times over 61 days and converted to no action, and proposed tracking the consecutive-failure count with a distinct escalation at N≥2 (a daily reminder, and an auto-managed tracking issue so the state persists where work is planned). The developer reviewed it and declined: the detector fired correctly every time, Telegram is the only push channel configured and delivered every message, so the failure was the decision to act, which is owned by the operator rather than automated around. The genuine detection gap is a different signal — an environment that has fallen behind `main`, which is the consequence that actually went unnoticed and is tracked in #730, unaffected by this. **(b) #732 — exposure review, answered NO.** Production ran the pre-#695 build (mass IDOR + privilege escalation on `/mcp`, #681) for 67 days. The review found the gap unexercisable rather than merely unexercised: production holds **one artist profile**, and #681 requires an authenticated user acting on a *different* artist's data. All three accounts were created 2026-03-19, three and a half months before the window, and **no account was registered during it** despite open self-registration. `app.log` sits on an unrotated Docker volume spanning 2026-03-16 → 2026-09-11 — full window coverage — with all 48 `module:"mcp"` entries being the startup line and no MCP errors at all. Retained nginx logs (2026-08-28 onward only) show nine `/mcp`-ish requests, all unauthenticated bot probes, none reaching a tool handler. Zero orders were created in the window, so `orders-by-artist` had no new buyer PII to expose. **Stated limitation:** nginx logs for the first ~54 days have rotated away, so that period has no request-level record; the verdict does not rest on them. **Surfaced by the review:** `/mcp` has no audit trail at all — `pino-http` logs only URLs starting with `/api` and `server/mcp.ts` logs errors without caller identity, still true after #695 — which is why answering this took structural inference instead of a log query. Filed separately rather than widened into #732. | Security |
| 2026-09-11 | Prune `.trivyignore.yaml` to zero entries; the partitioner replaces per-CVE suppression (#739) | #728 landed the blocking/vendored split with `.trivyignore.yaml` deliberately untouched, so Trivy kept applying all 50 suppressions *before* the report reached `script/partition-trivy.mjs` — every CI run since reported `0 blocking, 0 vendored` and the vendored warning table the feature exists to produce had **never rendered on a real run**. A gate that has never fired in the pipeline it guards is the same class of gap as the postmortem it came from. **Method:** the production image was scanned with the ignore file not applied (ground truth, 32 CRITICAL/HIGH findings), each of the 50 entries classified against that report, and the prune verified by re-scanning with the pruned file — partitioner output byte-identical to the unsuppressed baseline, so the prune provably removed suppressions only for findings already classified non-blocking. **Result: all 50 deleted, none kept.** 32 are matched by the two vendored path globs (10 in the base image's bundled npm, 22 in drizzle-kit's esbuild Go binary); 18 were dead entries the gate no longer sees — 12 absent from the image at any severity (the base image's npm bundle moved on; top-level esbuild is now 0.28.2, past the `GHSA-gv7w-rqvm-qjhr` range) and 6 still present but rated MEDIUM, below the CRITICAL/HIGH threshold, and on the drizzle-kit binary so vendored if ever re-rated. **The expected survivors did not survive:** the 8 *globally*-scoped esbuild Go-stdlib entries were the reason to do this carefully — global suppressions could have been masking findings on our top-level esbuild or `@esbuild-kit/esm-loader`'s copy, which are ours to bump and must block. The scan shows all 8 flagged only on the drizzle-kit binary, so no project-controlled esbuild finding was hidden and no #653/#652 work is unblocked by this. **Chosen:** keep the file, empty, with `vulnerabilities: []` and a header documenting the escalation order (upgrade → vendored path pattern → suppress, in that order) — rather than deleting it and dropping `trivyignores:` from both scan jobs, so a future suppression needs no workflow change and lands next to the reasoning about why it is a last resort. **Side effect:** #729 (warn before entries expire) now has nothing to warn about; it stays open as a guard for future entries, not a backlog of 50. **Rollback:** `git revert` restores all 50; a misclassification surfaces as an immediately red `main` on the next scan, never a silent unsuppression. | Security |
| 2026-09-11 | Pin `@types/node` to the runtime major (25.x) rather than taking Dependabot's 26.x (#741) | Dependabot PR #714 proposed `@types/node` 24.13.1 → 26.5.0, two majors in one step. All three Dockerfile stages run `node:25-bookworm-slim`, so 26.x types describe APIs the container does not ship — code would type-check locally and fail at run time, and `tsc` cannot catch it because the types *are* the contract. Chosen: `25.9.6`, exact-pinned to match the existing style, tracking the runtime rather than leading it. The rule this sets: `@types/node`'s major follows the base image's major, and the bump belongs in the same change as a base-image bump, not before it. #714 was closed rather than merged because its branch name pins the 26.x version; Dependabot re-proposes 26.x on its own once the image moves to `node:26`. Verified: `tsc` clean, 238 tests pass. The only other lockfile movement is `undici-types` 7.18.2 → 7.24.6, which `@types/node` owns. | Architecture |
| 2026-09-11 | Audit trail for `/mcp`, identifiers only, with log rotation (#738) | `/mcp` is an authenticated, mutating surface exposing 12 tools and 14 resources and it recorded **nothing**: `pino-http` skips any URL not starting with `/api`, and `server/mcp.ts` logged only errors, without caller identity — all 48 `module:"mcp"` lines in six months of production logs were the startup message. The #732 review therefore had to answer "was the pre-#695 mass-IDOR gap exercised?" by structural inference (one artist profile exists, no accounts registered in the window) rather than by querying logs. That worked only because production has one artist; with two the same question becomes unanswerable. **Three decisions.** **(a) Identifiers only** — `userId`, `sessionId`, kind, name, `targetId`, outcome, duration. Never argument values: `/api/admin/logs` and the `get_logs` MCP tool both surface `app.log` to admins, so logging `create_order` arguments would put buyer contact details in a file `get_logs` hands back — a milder replay of the #681 exposure this trail exists to detect. Enforced by a sentinel-value test, not by care. **(b) Rotation in the same change** — `app.log` was unbounded on the `artverse-production_logs` volume and per-call logging makes a steady writer of it; an audit trail must not become the reason a volume fills. 10 MB × 10 files. `pino-roll` is used as a **stream, not a `pino.transport()`**: transports spawn worker threads that cannot resolve modules inside the single-file CJS bundle, the same constraint that already shaped `pino.multistream`. Since pino-roll resolves asynchronously and CJS has no top-level await, writes pass through a `PassThrough` piped into the destination once ready, falling back to a single unbounded file if rotation fails — losing rotation is recoverable, losing file logging is not. **(c) `autoLogging` extended to `/mcp`** — so requests rejected before dispatch (unauthenticated probes, session-binding 403s) leave a trace; #732 had to fall back to nginx logs for exactly this, and those had rotated away for 54 of the 67 days. **Implementation:** instrumentation patches the registration methods (`server/mcp-audit.ts`) rather than editing 30 handlers, so a tool added later is audited by construction; every refusal routes through `recordDenial()`, including the `orders-by-artist` resource and the `order_summary` prompt, which shape their own refusals. **Correction found while building:** the plan assumed prompts were read-only templates not worth auditing — `order_summary` in fact reads buyer PII and has its own authorization check, so prompts are audited too. **Rejected:** logging argument key names (widens the review surface each time a tool gains a parameter, for forensics the target id already provides). **Consequence to accept:** history is now bounded by size rather than time, so a traffic burst shortens the window the trail covers. | Security |
| 2026-09-11 | Standing policy file as the mechanism for agent autonomy (#744) | The developer's stated goal is to spend as little of their own time on software development as possible: issues get filed, agents work them to staging, production stays gated. The obstacle is not capability but interruption — every question an agent asks forces the developer to page in context they did not choose. **Chosen:** `specs/AGENT-AUTONOMY-POLICY.md`, standing answers to the question classes agents actually ask, harvested from rules already scattered across `CLAUDE.md`, `specs/SECURITY_AGENT.md` §6, the `.trivyignore.yaml` header, existing decision-log rows, and developer preferences that until now lived only in per-session model memory — which no other agent can read. The load-bearing mechanic is the **write-back rule**: every escalation the developer answers becomes a rule in the file in the same PR, so the escalation rate decays toward zero and the same question is never asked twice. Paired with **default-and-document**: where the policy is silent, the agent picks the most reversible option, ships it, records it, and proposes the rule — because a revert costs minutes while a blocking question costs an afternoon. **Work is gated by reversibility, not sensitivity** — an earlier draft tiered by how sensitive a change looked, which gates interesting work rather than dangerous work. Exactly five things need a human: destructive schema/data operations (because `rollback-production.yml` restores the image, not the database), production promotion, secrets, anything that spends money or reaches third parties, and deleting anything not in git. Auth code, PII-adjacent logging, nginx config and security suppressions are deliberately **not** gated: all are revertible. **Deliberately not done in this PR:** relaxing the plan-approval step in `CLAUDE.md`. The proposal makes enforcement mechanical (a CI check failing any PR that touches a gated path without a human label), and that check does not exist yet — dropping the human gate before its replacement exists would leave no gate at all. CLAUDE.md points at the policy and says so explicitly. **Evidence base:** the 2026-09-11 session, where five pieces of work shipped, every plan was approved unchanged, and all three genuine problems surfaced during execution rather than planning — a plan assumption that was factually wrong (#738), a green pipeline that verified nothing (#628), and a deliberate deviation from the issue text (#742). Plan approval caught none of them, which is why the policy invests in verification-on-staging and a context-independent reviewer instead. | Process |
| 2026-09-11 | Enforce the autonomy gated list mechanically, and say plainly what it cannot see (#744) | The autonomy policy lets agents merge without plan approval; that is only safe if the short list of irreversible changes is enforced by something other than agent self-assessment. **Chosen:** `.github/workflows/gated-paths.yml` + `script/gated-paths.mjs`, failing any PR with a gated change until the developer applies `human-approved`. **The detector's difficulty is over-gating, not under-gating** — every false positive costs the developer exactly the time this whole policy exists to save. Migration SQL is therefore stripped of comments, split on `;` and drizzle's `--> statement-breakpoint`, and matched on each statement's **leading keyword**; a naive text search flags every additive migration in the repo, because each one carries `ON DELETE no action ON UPDATE no action` inside its foreign-key constraints. Validated against all 14 real migrations: it flags exactly the three that backfill or tighten data (0007's `is_published` backfill from #513, and 0008/0010's slug backfill plus `SET NOT NULL`, the #543 failure shape) and passes the other 11 untouched. **Honest scope:** only 2 of the 5 gated items are visible in a diff — destructive schema/data, and secret-shaped paths. Production promotion is a `workflow_dispatch`; spending money or reaching a third party is not inferable from a diff; deleting things not in git is by definition absent from it. The failure message says so, because a gate believed to cover more than it does is worse than no gate. **Self-guarding:** the script, its workflow and `AGENT-AUTONOMY-POLICY.md` are themselves gated paths — without that, an agent could widen its own authority in a single PR and make every other rule unenforceable. **Label authenticity:** a label alone proves nothing, since an agent with a token can apply one, so the job reads the issue timeline and rejects an approval applied by a Bot account. Known limit, documented rather than papered over: an agent using the developer's own credentials is indistinguishable from the developer. **Own workflow, not a job in `ci.yml`:** the check must re-run when a label changes, and adding `types: [labeled]` to `ci.yml` would re-run a full Docker build and two Trivy scans on every label edit. | Process |
| 2026-09-11 | Make the gated-path check required, and stop self-guarding the policy document (#744) | Two corrections after watching the gate run on real PRs. **(a) It was not blocking anything.** `main`'s branch protection required only `Lint, Type Check, Test & Build`, so #746 and #747 both merged with `Gated Path Review: fail` — the gate correctly identified self-guarded changes, refused to pass, and the repo's `gh pr merge --auto` merged them regardless. `Gated Path Review` is now in `required_status_checks`; native auto-merge honours required checks, so that single setting closes the hole. Deliberately done *after* fixing the file-list crash rather than before: a mandatory gate that can crash blocks every merge in the repository, which costs the developer far more than it saves. **(b) Self-guarding `AGENT-AUTONOMY-POLICY.md` fought the policy's own write-back rule.** §11 requires that file to be edited every time the developer answers an escalation — the mechanism by which the escalation rate decays — so guarding it would bill a human label for each one, which is precisely the cost the policy exists to remove. It bought nothing in exchange: enforcement is the path list inside `script/gated-paths.mjs`, so editing the prose cannot widen what CI blocks. The self-guard now covers the detector and its workflow only. **Accepted residue:** every future change to the gate mechanism itself still costs one label. That is correct — without it an agent could disable the gate in a single PR — and it should be rare. | Process |
| 2026-09-11 | Post-deploy evidence posted to the issue; staging log smoke test repaired after #738 (#744) | Two things, both about the same weakness: a pipeline that reports success without evidence anyone reads. **(a) A latent break introduced four hours earlier.** #738 moved logging onto `pino-roll`, which writes `app.<n>.log`; the staging smoke test asserts on `/app/logs/app.log`. It stayed green only because staging's volume still holds the 1.7 MB pre-#738 file — meaning that since #738 the test had been validating the **previous build's** log, and on a fresh volume it would fail outright. Exactly the class #628 belongs to, introduced by the change that was meant to teach the lesson. The step now resolves the newest file the same way `logReadPaths()` does, and additionally asserts `module:"mcp"` entries exist, so the audit trail's wiring is checked rather than assumed. **(b) Evidence where it will be read.** An autonomous pipeline merges its own work, so the record that a change actually reached staging must land on the issue, not in a job log nobody opens — the gap the #732 review hit, where CI said "deployed" and answering "did it work?" still meant an SSH session. The deploy job now comments on every issue referenced in the merge commit with the commit, the run link, the version staging reports, and which smoke tests passed. Uses `GITHUB_TOKEN` with `issues: write` — no new secret, so nothing on the gated list. **Deliberately modest claim:** the comment says the build is live on staging, *not* that the change's own behaviour was exercised. Issue-specific verification stays a separate act; a comment that overstated what it proved would be worse than none. | Process |
| 2026-09-11 | Activate Tier B: plan approval drops for issues a human labels `agent-ready` (#744) | The policy (#745) and its mechanical gate (#746, #747, #748) were built first and deliberately changed no workflow; this is the switch. **Three tiers, one label.** Tier A (escorted) stays the default and is today's workflow verbatim; Tier B (autonomous to staging) applies only to an issue carrying **`agent-ready`**, which a human applies — an agent may never promote its own issue, under the same timeline check that already rejects a bot-applied `human-approved`. Tier C (gated) is unchanged and cuts across both. Tier B removes plan approval, priority confirmation, mid-work clarification, merge approval and staging-deploy approval; it removes nothing from the decision log, the documentation rules, or what "done" means. **The finding that made this more than a rename:** `auto-merge.yml` enabled auto-merge on *every* non-draft PR from the repository owner the moment CI went green, so Tier A's approval step was already unenforced at the merge layer — every PR in this issue's own series self-merged. It is now conditioned on `agent-review` (Tier B) or `autorelease` (the release PR), with `labeled` added to its triggers because the label normally arrives after the PR opens. Accepted consequence: a human-opened PR no longer merges itself; apply `agent-review` or merge by hand. **`auto-merge.yml` added to the self-guarded paths** — not asked for, but conditioning it made the file load-bearing, and an agent that could quietly restore the unconditional form would re-grant Tier B to every PR in one edit. One line to revert if that proves over-tight. **`agent-stuck` created** as a state distinct from `agent-failed`: the policy told agents to apply a label that did not exist, and the repo's only nearby label was terminal. Stuck means a question is outstanding and the work is sound; failed means the run is dead. Collapsing them loses the only signal that says whether a human must act. **Defaulted, not asked (policy §2):** three consecutive red CI cycles end a run — one strike hands back work a re-run fixes, five means the agent is guessing past the point of usefulness. **Activated before the reviewer agent and issue-specific staging verification exist,** which #744 names as two of the three replacements for developer judgement. The argument accepted: Tier B's blast radius is exactly the set of issues the developer labels by hand, which is a stronger control during a trial than a reviewer agent would be, and it produces the trial's four numbers while those are built rather than after. The argument declined: build them first, at roughly a week of throughput and zero risk. | Process |
| 2026-09-12 | Make `PR Contract` a required check, after exempting the two PR kinds that can never satisfy it (#752) | `PR Contract` (#749) had been failing and passing correctly on real PRs while nothing consumed its verdict — `main`'s branch protection required only `Lint, Type Check, Test & Build` and `Gated Path Review`, so a PR that omitted its issue reference, its verification or its CI-coverage statement merged anyway. The same hole #748 closed for the gate, and it matters more under Tier B (#751), because the contract is the mechanism that forces an autonomous run to state what CI does *not* cover — the #628 lesson. **The blocker found before flipping it, not after:** a required check a PR can never satisfy is a permanent block rather than a gate, and two kinds of PR here can never satisfy it. Dependabot writes its own body from the dependency manifest — a real one fails three of the four rules — so every dependency update and `dependabot-auto-merge.yml` with it would have stalled indefinitely. `release.yml` generates the release PR body from the changelog, and its only issue references sit inside an HTML comment that `prose()` strips by design, so releases would have stalled too. Both are now exempt by author and by the `autorelease` label, passed in from the event payload rather than a second API call. **An exempt PR reports "skipped", never a green tick** — a required check must not look like a review it did not perform. Ordering was deliberate and mirrors #748: land the exemption, then flip the requirement, never the reverse. **Also recorded here rather than left in a comment thread:** a conflicted PR runs no `pull_request` workflows at all, because GitHub cannot build the merge ref — they do not fail, they are absent, while `push` and `pull_request_target` checks carry on looking green. Seen on #751 when #749 and #750 merged underneath it. Not a bypass (a required check that never reports blocks the merge, and a conflicted PR cannot merge regardless), but an agent reading a green summary would report work as verified that the gate never saw, so `AGENT-PIPELINE.md` now names missing-checks as a distinct failure state whose fix is rebase. | Process |
| 2026-09-12 | Dispatcher: `agent-ready` starts an unattended run (#753) | #744 removed the plan-approval step but not the session — a human still had to open a terminal and tell an agent to work each issue, which is most of the cost the policy exists to remove. **Chosen:** event-driven, `on: issues: labeled`, over a scheduled sweep. The latency is the point: the developer labels an issue and sees movement, rather than wondering whether the poll has run. A sweep stays the fallback if event filtering proves unreliable. **Second caller of an existing capability, not a new one:** `doc-agent.yml` has run `anthropics/claude-code-action` against `ANTHROPIC_API_KEY` since 2026-03-12, so the spend decision was taken long ago; what this adds is a *bound* on it rather than a new question — a 45-minute job timeout and `MAX_RUNS_PER_DAY`, because a runaway loop plus an unbounded dispatcher is an invoice rather than an incident. **Four guards, each from a specific failure:** the label filter; a `sender.type == 'User'` check, because an agent that can self-label grants itself Tier B and makes the tier boundary decorative; a refusal when `agent-working` is already present, so two runs never race one issue; and the ceiling. **`concurrency: agent-run` with `cancel-in-progress: false`** — one run at a time repository-wide. #744 risk 5 predicted that concurrent runs collide on `DECISION-LOG.md` (append-only) and `package-lock.json` (regenerated) every time, and #749/#750 then conflicted #751 for real, which also silently skipped its required checks. **Checkout and the action both use `RELEASE_PAT`, never `GITHUB_TOKEN`** — a PR opened with `GITHUB_TOKEN` does not trigger `pull_request` workflows, so `Gated Path Review` and `PR Contract` would never run on agent PRs. That is the #752 finding in a permanent form rather than a conflict-dependent one, and it would have made the gate decorative on precisely the PRs it exists for. **Self-guarded** in `script/gated-paths.mjs`: this file holds the label filter, the human check, the concurrency group and the ceiling, so an agent able to edit it could remove all four in one PR with nobody watching. It is the strongest case on that list. **Known limit, carried over unchanged:** an agent acting with the developer's own credentials is indistinguishable from the developer; the guard rejects bot accounts, not impersonation. **Reversible:** delete the workflow file and labelling an issue does nothing again. | Process |
| 2026-09-14 | CI Claude auth moves to `CLAUDE_CODE_OAUTH_TOKEN`, and the doc agent fails closed on a missing credential (#760) | `ANTHROPIC_API_KEY` has been registered since 2026-03-12 holding an **empty value**, so `anthropics/claude-code-action` has never once executed in this repository. Two consequences, found together. **(a) The dispatcher could not run.** #755 shipped on 2026-09-12 and the first real dispatch (issue #688) passed all four guards, claimed the issue, checked out, and died 11 seconds in on `Either ANTHROPIC_API_KEY, CLAUDE_CODE_OAUTH_TOKEN, or workload identity federation is required`. Tier B has been mechanically active since #751 without ever dispatching. **(b) The Documentation Agent has been a green check over nothing.** Its enforce job gated Claude on `ANTHROPIC_API_KEY != ''`, skipped when false, and reported **success** regardless — so every PR in the #744 series merged past an enforcement step that never executed, while `CLAUDE.md` stated undocumented changes would block merge. The #628 class again, and the third instance after #752 and #631. **This also corrects the record.** #753's own row here asserts that "`doc-agent.yml` has run `anthropics/claude-code-action` against `ANTHROPIC_API_KEY` since 2026-03-12, so the spend decision was taken long ago" — that premise was false, and it was load-bearing in the argument that the dispatcher added only a bound on existing spend rather than new spend. The dispatcher is in fact the first thing in this repository to spend anything on the action. **Chosen: `claude_code_oauth_token`, not a fresh API key.** The pinned action accepts either. A subscription-backed token draws on the existing Claude allowance rather than opening a per-token API invoice, which is the intended cost model for CI runs here. **Accepted consequence, stated rather than discovered later:** that allowance is the *same* one interactive Claude Code sessions draw on, so at `MAX_RUNS_PER_DAY: 10` and a 45-minute job timeout, dispatch can throttle the developer's own work. The ceiling is now a cost decision, not an inherited number. **Fail closed, with two carve-outs that are structural rather than convenient.** A same-repo PR with no credential is a misconfiguration and now goes red; the credential swap alone would only have hidden the defect until the token expired. Fork PRs and Dependabot still skip, because GitHub withholds secrets from untrusted heads and Dependabot runs against a separate store — failing them would block every external contribution and every dependency bump. **Dependabot needs its own test:** its branches are same-repo, so `head.repo.fork` is `false` and a fork-only check would have turned #756, #758 and #759 red on merge. **Verified in both directions on the PR itself**, not asserted: one commit carried the fail-closed check while still pointed at the empty secret and went red with the intended error; the next carried the swap and ran the enforce step for the first time. **Split across two PRs** because `doc-agent.yml` is not on the gated list and `agent-dispatch.yml` is — bundling would have held the fix with per-PR consequences behind a `human-approved` label it does not need. **Known residue:** nothing warns before the OAuth token expires. When it lapses this check turns red rather than silently passing, which is the improvement, but it is still after the fact — the shape #729 exists to fix. | Process |
| 2026-09-14 | Upgrade `libpcre2-8-0` in the Dockerfile rather than suppressing it (#763) | `Build & Trivy Container Scan` blocked `Deploy to Staging` on exactly two blocking findings — CVE-2026-86145 and CVE-2026-89161 in `libpcre2-8-0` `10.42-1`, both fixed upstream in `10.42-1+deb12u1` — while the other 30 HIGH in the same scan partitioned as vendored and only warned, which is #728 working as designed. **Chosen:** append `libpcre2-8-0` to the existing `--only-upgrade` statement in the run stage, the same remedy #710 applied to `libgnutls30` and in the same `RUN` layer, so no new layer is added and the fix self-cancels once upstream rebuilds the base image. **Not suppressed:** #728 made OS packages block regardless of path precisely because a Dockerfile upgrade can fix them, and #710 deleted rather than date-extended the five lapsed `libgnutls30` suppressions for the same reason — a `.trivyignore.yaml` entry here would mask a fixable HIGH and re-enter the loop those two decisions exist to end. **Verified before the change was written**, by running the upgrade against the unmodified base image: `node:25-bookworm-slim` ships `10.42-1` and `apt-get install -y --only-upgrade libpcre2-8-0` yields `10.42-1+deb12u1`, the exact version Trivy names as fixed. The CI scan remains the authoritative verdict, since it runs the #728 partitioner against the live advisory DB rather than a local snapshot. **Caught the day it appeared, deliberately.** The same mechanism in #710 went unnoticed from 2026-07-13 and left staging serving a 2026-07-10 build for roughly two months; here the block surfaced because #761 and #762 merged and did not deploy. That detection path is incidental rather than designed — nothing alerts on a blocked staging deploy, which is the gap #730 covers. | Security |
| 2026-09-14 | Grant the dispatcher's agent an explicit tool allowlist, and fail the job when a run produces no PR (#765) | #762 fixed the dispatcher's authentication and the first real run then succeeded at doing nothing: 20 turns, `permission_denials_count: 12`, no branch, no PR, no comment, job green, and `agent-working` left behind so guard 3 refused every retry until a human cleared the label. **Cause:** `claude_args` carried only `--max-turns 80`, and the action supplies the base GitHub tools and nothing else (`docs/configuration.md`, "the base GitHub tools are always included"), so the prompt instructed the agent to branch, commit, push and open a PR while the harness denied every call that would do it. The run could not have succeeded as written. **Chosen: a named allowlist**, not `--permission-mode bypassPermissions` — the blast radius should stay set by `RELEASE_PAT` and the gated-path check rather than by removing the tool boundary, and the list widens only on an observed denial, with `permission_denials_count: 0` as the acceptance signal. **This is the first change that lets an unattended agent write.** Every prior run was harmless because it was denied everything; the remaining controls are the gated-path check, the required PR contract, `MAX_RUNS_PER_DAY: 10` and the 45-minute timeout. Flagged as the reason to decline and approved knowingly. **Second defect, independent of the first:** `Work the issue` was the last meaningful step, so nothing asserted a work product existed — the job reported success over an empty run *and* stranded the issue behind its own claim. A final `always()` step now fails the job when no PR exists, releases `agent-working`, applies `agent-failed` and comments the run URL. This restores a check the pre-#753 VPS runner had and the move to Actions dropped; #691 and #692 still carry its output, "no commits were produced". **Detection is by head branch, not PR body, and that distinction was found by testing rather than reasoning:** matching `#<issue>` in PR bodies reports that #688 produced work, because #762's body discusses the failed #688 run at length. Verified against the live PR list — body matching returns 1 for #688, branch matching returns 0, and neither matches issue #76 against `#763`/`#766`. The branch name is the contract (the prompt mandates `feature/issue-<n>-<slug>`, `branch_prefix` pins it), so a misnamed branch reads as failure — a loud false negative, the safe direction when the alternative is a silent false positive. **Known limit, recorded rather than papered over:** an agent that correctly concludes no change is needed is indistinguishable from one denied every tool, and both are now labelled `agent-failed`. Fixing that needs a signal from the agent, not an inference from a missing PR. | Process |
| 2026-09-14 | Four corrections to the dispatcher after its first supervised trial (#765) | The trial dispatch on #731 never reached the agent, and exposed three defects in #767 plus one that predates it. **(a) The ceiling counted invocations, not work.** `total_count` counts every run of a workflow triggered on `issues: labeled` — that is every label applied anywhere in the repository, most of which the job-level `if` then skips. It read 13/10 and refused the trial while exactly one run had done any work that day: 9 skipped, 2 cancelled, 1 success, 1 refused. Filing the three issues from this session had consumed the agent budget. The ceiling exists to bound spend and a skipped run spends nothing, so it now counts only runs that reached the agent; in-progress runs carry a null conclusion and still count, preserving "including this one". Measured against live data before shipping: the same query returns 2 where the old one returned 13. **(b) The refusal was silent.** That comment had never posted — the step runs before checkout, so `gh` had no git remote to infer and failed with "not a git repository". The developer saw a label that did nothing and no explanation. `--repo` added. Worth noting the shape: the ceiling and its notification failed together, so the bound was enforced invisibly. **(c) `always()` was too broad.** #767's new confirmation step fired after the ceiling refused a dispatch, and tried to release a claim that was never made and mark an untouched issue `agent-failed`. Now gated on `steps.claim.outcome == 'success'`. **(d) The failure path was inert.** That step used `RELEASE_PAT`, which cannot write labels — "Resource not accessible by personal access token (addLabelsToLabelable)". So even on a genuine empty run it could not have released the claim or applied `agent-failed`; the job would have failed loudly and left the issue stranded exactly as before. Switched to `GITHUB_TOKEN`, which the job already grants `issues: write` and which the claim step uses successfully. **(c) is the reason #731 escaped undamaged** — the mislabel was attempted and rejected. Two bugs cancelling is luck, not a control. **What this says about the trial:** it cost one refused run and found four defects, three of them in the change made one hour earlier, none of which CI could have caught — `agent-dispatch.yml` triggers on `issues: labeled` and no `pull_request` check executes a line of it. The supervised trial *is* the test environment for this file, and should be treated as a required step for changes to it rather than a formality. | Process |
| 2026-09-14 | Document that `RELEASE_PAT` cannot write to issues at all, found while working #731 | (d) in the row above narrowed this to labels, from the `always()` failure-path step. Working #731 found the actual boundary is wider: every issue-side write from the agent's own session — `gh issue edit --add-label`, `--remove-label`, `gh issue comment`, `gh issue close` — returns `403 Resource not accessible by personal access token` on both the GraphQL and REST paths, while `Contents: write` (checkout, push) and `Pull requests: write` (PR create, PR labels, PR comments) both work. Confirmed general rather than #731-specific by testing against a fresh throwaway issue (#770) and by hitting the plain REST endpoints directly (not only `gh`'s GraphQL path). Issue *creation* itself still succeeds, which is what makes the gap easy to miss until a label or comment is attempted against an existing issue. **Consequence:** steps 5 and 8 of `AGENT-PIPELINE.md` §2 — drop `agent-working`, apply `agent-done` — cannot be performed by the agent for this run, and this run's status update went to the PR as a comment instead, with the gap stated explicitly rather than silently skipped. **Not fixed here:** widening `RELEASE_PAT`'s scope to include `Issues: read and write` is a credential change (policy §1 item 3), gated, and requires a human with access to the PAT's GitHub settings — recorded as a known limit in `AGENT-PIPELINE.md` §2 rather than worked around. **Also disclosed:** narrowing this down created one piece of litter, throwaway issue #770, which the same gap means the agent cannot close itself — left for a human alongside the scope fix. | Process |
| 2026-09-14 | `.trivyignore.yaml` suppressions naming an external fix must record how it was verified, plus a fallback (#731) | Postmortem action item #7 (`docs/postmortems/2026-09-10-trivy-scan-blocked-staging-61-days.md`): five `libgnutls30` entries named #629 ("base-image rebuild") as the real fix, and that fix was never verified — `node:25-bookworm-slim` still shipped `3.7.9-2+deb12u6` months after Debian bookworm had `3.7.9-2+deb12u7`. Anyone who picked up #629 would have rebuilt, seen no change, and had no documented next step. **Chosen:** extend the header convention in `.trivyignore.yaml` (mirrored in `specs/SECURITY_AGENT.md` §6) so a `statement` naming an external fix must also state how that fix was confirmed available right now (exact upstream version, where checked) and a concrete fallback if upstream still hasn't acted by `expired_at`. **Scope, declared rather than assumed:** the issue's own success criterion asks that the convention be "applied to existing entries that name an external dependency" — `.trivyignore.yaml` is `vulnerabilities: []` (emptied in #739; nothing to retroactively edit there). `.github/trivy-vendored-paths.json` does name upstream actions in its two `reason` strings ("fixable only by an upstream Node base-image rebuild", "fixable only when drizzle-kit bumps its esbuild range"), but that file is a structurally different, non-expiring mechanism (§5): a vendored pattern is permanent by definition — "no lockfile bump, npm override, or Dockerfile line can change its version" — so there is no `expired_at` cycle for an unverified promise to lapse against, which is the specific failure this issue is about. Left it alone rather than importing a verify/fallback shape that does not fit, and noted the distinction here so the next reader does not re-propose it. | Process |
| 2026-09-15 | The workflow owns the agent label lifecycle; `RELEASE_PAT` scope left alone (#771) | The first successful Tier B run (#731 → #769, merged and deployed) still ended with `agent-working` stranded on the issue, because `RELEASE_PAT` cannot write to issues at all — every `addLabelsToLabelable` / `removeLabelsFromLabelable` / `addComment` / `closeIssue` returns 403 on both GraphQL and REST, while issue *creation* succeeds, which is what made it easy to miss. The agent found this itself mid-run and, correctly, declined to widen its own credential (policy §1 item 3) and reported instead. **Three options weighed.** Widening the PAT is simplest and was rejected twice over: it is a credential change, so it puts the developer in the loop for the fix and for that token's scope forever, and it preserves the weaker design in which the agent declares its own outcome — a run that *cannot* declare is indistinguishable from one that chose not to, which is exactly how #731 looked. A second issue-only token trades one permanent broad credential for two secrets and keeps the same self-reporting. **Chosen: move every issue-side write into the workflow, under `GITHUB_TOKEN`** — a capability already demonstrated in this same job by the claim step and by #768's failure path, so nothing new is granted. **The agent keeps its voice through the PR:** it can label PRs (it applied `agent-review` to #769 unaided), so it signals `agent-review` / `agent-stuck` / `agent-failed` there and `agent-dispatch.yml`'s final step mirrors that onto the issue; `agent-failed` wins if both are present, because over-escalating is the safe direction. **The claim is now released on every path** — success, signalled failure, or no PR at all — so no route through the job strands an issue behind its own `agent-working`, the failure that cost #688 three manual clearances. **`agent-done` moved to the staging deploy**, gated on the issue still carrying `agent-ready`: that marker is applied by a human to start a run and nothing removes it, so it is a reliable "this went through the pipeline" signal at deploy time and keeps the loop off issues referenced by ordinary human PRs. Placing it there also makes the label honest — `agent-done` now means the build is live, which is the only moment that claim is true and precisely what #750's evidence comment already asserts. Accepted imprecision: an issue labelled `agent-ready` but then worked by hand also gets `agent-done`; harmless, and preferable to maintaining a second marker. **Mapping verified against live data before shipping**, the same discipline that changed the implementation in #768: PR #769's real labels yield no signal (claim released, outcome left to auto-merge), #688 yields no PR (terminal `agent-failed`), and synthetic `agent-stuck` / `agent-failed` / both map to `agent-stuck` / `agent-failed` / `agent-failed`. **Deliberately one PR** rather than the split used for #761/#762 — the `ci.yml` half is not urgent, only `agent-dispatch.yml` is gated, and splitting would have left `AGENT-PIPELINE.md` documenting half a protocol. **New failure mode, one-directional:** a claim released on a PR that is not really finished. Visible (the PR is open and unmerged) and recoverable (re-apply the label), where the mode it replaces was an issue nobody could dispatch until a human noticed. **Still open:** an agent that correctly concludes no change is needed produces no PR and therefore still reads as `agent-failed` (#765). | Process |
| 2026-09-15 | Add an `error` listener and explicit timeouts to the pg pool (#684) | `server/db.ts` constructed `pg.Pool` with only `connectionString` — no listener on the pool's `error` event, which node-postgres emits when an *idle* client goes bad (e.g. the backend restarting under it). An EventEmitter with no `error` listener throws, so that was an unhandled exception straight to a process crash on the next idle-client hiccup, with no upstream trigger needed. Defaults also left `connectionTimeoutMillis` at `0` (wait forever for a connection) and `max`/`idleTimeoutMillis` implicit. **Chosen:** `pool.on("error", ...)` logging through a new `dbLogger` child logger (matching the existing `authLogger`/`mcpLogger` pattern in `server/logger.ts`, per §5's "log identifiers, never values" — only the error object is logged, never query data), plus explicit `max: 10`, `connectionTimeoutMillis: 5_000`, `idleTimeoutMillis: 30_000`, `statement_timeout: 30_000`. All additive and reversible — no schema, no migration, no new env var; values are conservative defaults rather than something needing per-environment tuning yet. Covered by `server/__tests__/db.test.ts`: asserts the listener is registered, that emitting a synthetic pool error doesn't throw, and that the four options land on the constructed pool instead of node-postgres's defaults. | Architecture |
| 2026-09-15 | Raise the dispatcher's turn ceiling to 200 and stop instructing the agent to write to issues (#772) | Two defects with one symptom: every agent run so far has burned turns failing at an impossible instruction, then been truncated by a cap that was never calibrated. **(a) The prompt contradicted the design.** #773 moved the label lifecycle into the workflow and rewrote `AGENT-PIPELINE.md`, but left the workflow's own inline prompt telling the agent to "remove `agent-working`", label *issues* on escalation, and post its summary to the issue — every one of which 403s. #684 spent 10 permission denials on it and ended `is_error: true`. The prompt now states the constraint outright, tells the agent its PR labels are mirrored and its claim released for it, and routes human-readable status to a PR comment. Worth recording as a class: moving a responsibility out of a component is only half the change; the instructions that still name it are the other half, and they fail silently. **(b) 80 turns was a guess that every real run exceeded.** It was set before any run had ever reached the agent. #731 — documentation, chosen deliberately as the *easy* trial — used 82. #684 — server code plus a test — used 81 and was cut off mid-wrap-up. A cap that truncates every genuine run is not a bound, it is a defect. **Chosen 200, with the reasoning recorded rather than the number alone:** the actual bound is `timeout-minutes: 45` and `MAX_RUNS_PER_DAY`, which cap spend directly; those two runs used 16 and 8 minutes, so turns were binding long before time was. 200 leaves a backstop against a tight loop while letting ordinary work finish, and the timeout still catches a true runaway. **Not removed entirely,** which was considered: without any turn cap a degenerate loop could burn most of a 45-minute window before the timeout notices, and the backstop costs nothing when it is set above real usage. **Fixing (a) should reduce turn usage on its own** — both prior runs spent their tail retrying issue writes — so 200 is deliberately generous against a number that should now fall. Recalibrate from observed runs rather than adjusting it again by guess. | Process |
| 2026-09-15 | Exclude test fixtures from the DATABASE_URL check, then make `Gate` a required status check (#776) | The first autonomous code change merged in this repository (#774, opened and merged by the Tier B agent with no human present) turned `ArtVerse Security Checks` red on `main` and nothing stopped it. **Two problems, deliberately separated.** **(a) The check flagged a fixture.** `security-checks.sh` CHECK 6 scanned `server/` and `shared/` for `postgres://user:pass@host` with no test exclusion, and #774's new `server/__tests__/db.test.ts` sets `process.env.DATABASE_URL = "postgres://user:pass@localhost:5432/testdb"` — which is exactly what a pg test is supposed to contain. Excluded by **directory**, not by any attempt to judge whether a credential "looks fake": a check that guessed at realness would eventually guess wrong in the permissive direction. Real secret detection is unaffected — `Secret Scanning (gitleaks)` scans every file including tests and was passing throughout. Verified both directions before shipping: the current tree passes with the exclusion, and a planted `postgres://admin:...@db.example.com/prod` outside `__tests__` is still caught. **(b) Nothing consumed the verdict, which is the real finding.** `main` required only `Lint, Type Check, Test & Build`, `Gated Path Review` and `PR Contract`, so `auto-merge.yml` merged #774 with a security job red. This is the third instance of one pattern: #748 made `Gated Path Review` required after #746 and #747 merged past it, #752 did the same for `PR Contract`, and `Security` was the one left advisory. It mattered less when every merge had a human on it; under Tier B an agent merges on required checks alone, so an advisory check is one an agent is structurally free to ignore. **`Gate` is the context made required**, not `ArtVerse Security Checks` — `Gate` is `security.yml`'s roll-up job and fails if any security job failed, so requiring it covers Trivy, gitleaks and the dependency audit rather than one script. **Ordering follows #748 exactly and is not negotiable:** land the fix, confirm green on `main`, then flip the requirement. A required check that is currently failing blocks every merge in the repository, which costs far more than it saves — the same reason #752 landed the Dependabot and `autorelease` exemptions before making the PR contract required. **Accepted consequence, stated rather than discovered:** a blocking Trivy finding now halts all merges, not just deploys. #763's `libpcre2-8-0` disclosure would have done that yesterday. That is the correct trade against what #774 demonstrated, and #728's vendored-path partitioning is what keeps it bounded to findings we can actually act on. | Security |
| 2026-09-15 | Force tag mode so the Documentation Agent's verdict is actually published (#764) | #760 fixed the credential and the enforce job started executing — 9 to 11 turns of real analysis per PR — but published nothing: no comment, no review, no check-run output, on every PR since. The fingerprint was identical each run, `permission_denials_count: 1` against exactly one missing artefact, which pointed at a single denied call rather than a broken configuration. **Cause: the mode, not the permissions.** The action runs in *tag mode* when triggered by an `@claude` mention — it creates a tracking comment before the run and updates it after, owning the comment itself — and in *prompt mode* when given a `prompt:` input, where it creates no comment at all. `use_sticky_comment: true` was already set and is not the control: per the action's own `docs/usage.md` it governs how many comments the action posts, not whether one exists. In prompt mode there was nothing to stick to, so publishing fell to the agent, and that call was denied. The same shape as #765 — a prompt asking an agent for something the harness forbids — reached from the opposite direction. **Chosen `track_progress: true`** over granting the agent `Bash(gh:*)` and having it run `gh pr comment`: it moves publishing out of the agent's hands entirely, so a silent non-post is no longer a possible outcome. That is the same reasoning as #771, which took the label lifecycle off the agent because a run that *cannot* report is indistinguishable from one that chose not to. **Verified on the PR that made the change**, as in #761 — the enforce job runs on `pull_request` from the PR head, so a visible comment and `permission_denials_count: 0` were the pass condition rather than a configuration read. **Left undecided deliberately:** whether the Documentation Agent becomes a required check. `CLAUDE.md` claims "Undocumented changes will block merge", which has never been true — the job skipped entirely until #760 and published nothing until now. That is #776's question again, and requiring a check whose output nobody has yet read would be premature; the decision wants at least a few PRs of visible verdicts behind it. | Process |
| 2026-09-15 | A sixth agent state: `agent-no-change`, signalled by a workspace marker (#779) | #765 added a check that fails a dispatch producing no PR, and recorded its own limitation in the workflow: an agent that correctly concludes an issue needs no change is indistinguishable from one denied every tool, and both are labelled `agent-failed` — terminal, human reset required. Carried through #771 and #772 unfixed, because it did not matter while the dispatcher could not produce anything at all. It matters now that Tier B has completed #731 and #684 end to end: a stale issue, a duplicate, or one already fixed by another change is a real outcome that will occur, and today it would be recorded as a fault and page a human to investigate a run that did exactly the right thing. **A third state, not a reuse.** `AGENT-PIPELINE.md` already insists `agent-stuck` and `agent-failed` are different states that must not stand in for each other; by the same reasoning this is a third — nothing is blocked (not stuck), nothing broke (not failed), nothing shipped (not done). Hence `agent-no-change`. **Signalled by a workspace file, not a PR label.** Every other outcome travels on the PR, which the agent can label; here there is no PR, and the agent cannot write to issues at all (#771). `.agent-outcome` is read by the confirmation step in the same job and the same runner — no credential, nothing that can 403. **Absence means failure, deliberately.** A denied, crashed or truncated run writes nothing and takes the old `agent-failed` path unchanged; the file is read with a bounded `head -c 4096` and its first line matched exactly, so junk, an empty file, or the marker buried below a preamble all fall through to terminal rather than being read generously. This branch decides whether a human is paged, so it errs terminal. Replayed across eight file states before shipping — absent, valid, marker-only, junk, empty, trailing whitespace, marker-not-first-line, CRLF — and each took the intended path. **The prompt says "do not invent work to avoid it".** An agent that learns an empty run is recorded as failure has an incentive to manufacture a PR, which converts a reporting defect into fabricated work — strictly worse than the bug being fixed. **The agent still never closes the issue.** Whether an issue was valid is the developer's judgement, not the run's; the label and a reasoning comment put it in front of them. **Honest gap:** this is the one outcome not yet demonstrated on a live dispatch, because an honest test needs an issue that genuinely requires no change and staging a fake one would prove nothing. The default path is unchanged and already proven, so the cost of waiting for a natural occurrence is that we keep today's behaviour until then. | Process |
| 2026-09-15 | Add a SIGTERM/SIGINT graceful-shutdown handler; add an `app` compose healthcheck to all four Compose files; defer the blue-green flip (#686) | Node's default `SIGTERM` behavior is immediate termination — `docker-entrypoint.sh` `exec`s node, so a signal from `docker compose up -d` (or any orchestrator) hits the process directly, killing requests mid-handler, e.g. between `createOrder` committing and its `201` response reaching the buyer. **Chosen:** extracted the handler into `server/shutdown.ts` (`registerGracefulShutdown(httpServer, pool)`) rather than inlining it in `server/index.ts`, purely so it is unit-testable without booting the real HTTP/DB stack — `index.ts` wires it to the real `httpServer` and the shared `pool` from `server/db.ts`. On `SIGTERM`/`SIGINT`: stop accepting new connections, call `closeIdleConnections()` to drop idle keep-alive sockets immediately (they don't hold up in-flight work but can hold the server open indefinitely), let in-flight requests finish via `httpServer.close()`'s own callback, close the shared pg pool, exit — with a 10s force-exit fallback so one hung request can't wedge a deploy forever. Idempotent (a second signal mid-shutdown is a no-op). `connect-pg-simple`'s own session-store pool (`server/replit_integrations/auth/replitAuth.ts`) is untouched — it isn't exposed for closing and `process.exit` ends it regardless; wiring it in would add surface for no observable benefit. **Also added:** a compose-level `healthcheck:` on the `app` service (root `docker-compose.yml` plus `deploy/{staging,production,preview}/docker-compose.yml`) hitting `GET /health` via `node -e` (the runtime image has no `curl`), matching the existing `db` healthcheck's shape. **Deliberately deferred:** the issue's own "longer term" fix — a two-container blue-green flip so the old container keeps serving until the new one is verified healthy — is a bigger architecture change than one issue should carry bundled with a reliability fix; recorded as a known limit in `specs/architecture/CONTAINERS.md` rather than attempted here. The new compose healthchecks are a step toward that flip (something has to report ready before a flip can gate on it) but do not by themselves close the 502 window on a single-container replace. **Verification:** `server/__tests__/shutdown.test.ts` drives the extracted handler directly (drain + idle-close + pool-close + non-zero exit on a close error + the forced-exit timeout, via fake timers) — this could be asserted in CI. Sending a real `SIGTERM` to the built `dist/index.cjs` and observing an in-flight request finish was attempted by hand but the sandbox this PR was authored in blocks spawning a listening process even with `dangerouslyDisableSandbox`; that observation is unverified beyond the unit tests and should be spot-checked on staging after merge. | Architecture |
| 2026-09-15 | Grant the dispatcher's agent unenumerated `Bash`; the enumeration was never a containment boundary (#782) | The third Tier B trial (#686 → #781) was the first dispatch substantial enough for the tool allowlist to bite, and it bit on **verification**, not convenience. The agent shipped a graceful-shutdown fix it could not exercise — no `SIGTERM` sent to the built binary with a real in-flight request, four compose healthcheck blocks never validated — and said so in its own PR: "the sandbox this PR was authored in blocks spawning a listening network process and blocks `docker` invocations outright". That honesty is the system working; the reason it had nothing to check with was a list. **Denials rose rather than fell.** #775 removed the impossible issue-write instructions expecting zero; #686 recorded 23 against #684's 10, because denials track how much a task needs to explore and confirm, not how much it is told to do — 12, 11, 10, 23 across the four runs. **The enumeration was already not a boundary, which is the part worth recording.** The list permitted `Bash(npm:*)` and `Bash(npx:*)`, and `npm run <script>` executes whatever `package.json` defines while `npm install` runs postinstall hooks: arbitrary code execution was granted from the start. What the list actually did was block `cat`, `docker`, `curl` and `node` while leaving that door open — it stopped the agent checking its work and contained nothing. Replaced with plain `Bash`, and the workflow comment now says explicitly not to re-add the enumeration in the belief that it contains anything. **The real bounds, named so they are not confused with the list:** an ephemeral GitHub-hosted runner, `RELEASE_PAT`'s scope — which cannot even write to issues (#771) — `Gated Path Review`, the required `PR Contract` and `Gate` checks, `MAX_RUNS_PER_DAY: 10`, and the 45-minute timeout. Those are load-bearing; the tool list was not. **`WebFetch` and `WebSearch` deliberately stay denied.** They do not help an agent verify a change and they widen egress rather than verification surface; add them only against a concrete need. **Prediction on record, because the last one was wrong:** the target is `permission_denials_count: 0` on the next substantial dispatch, and the number gets reported either way. | Process |
| 2026-09-15 | Escape `<`/`>`/`&` in JSON-LD after `JSON.stringify` before embedding in the SSR `<script>` tag (#683) | Stored XSS: `JSON.stringify` does not escape `<`, `>` or `&`, and the JSON-LD payload embeds attacker-controllable DB fields (artist name/bio, artwork title, gallery name, blog title). A bio of `</script><script>...` closed the tag early and ran for every visitor of that page. Every other meta-tag replacement in `injectMetaTags` already goes through `escapeHtml()`; the JSON-LD block was the one path that skipped it. **Chosen:** a dedicated `escapeJsonForScript()` applied only to the JSON-LD output, unicode-escaping `<` `>` `&` plus ` `/` ` (valid in JSON, invalid as raw script-context text) — representational only, a JSON parser reads it back to the identical object. Rejected: reusing `escapeHtml()`, which collapses whitespace and HTML-entity-escapes rather than JS-string-escapes — wrong transform for a script body, and would have mangled JSON containing multi-line text fields (bios, descriptions). | Security |
| 2026-09-15 | Deploy job applies `agent-done` from the merged PR's head branch, not from `#N` in the commit message (#785) | On its first day in service the #773 lifecycle block marked #688 `agent-done` and stripped its `agent-ready` — an issue no run has ever produced a branch for. The trigger was #773's own commit body, which says "the failure that cost #688 three manual clearances": the block walked every `#N` in the message and marked any that carried `agent-ready`. **The same bug, found and fixed once already, shipped a second time.** #768 tested the dispatcher's "did the run produce work" check against live data and discovered that matching `#N` in PR bodies reports #688 as worked because #762's body discussed it; it switched that check to head-branch matching and recorded why. #773 then wrote mention-matching into `ci.yml` under an "accepted imprecision" note describing a narrower failure (an `agent-ready` issue worked by hand) than the real one (any mentioned issue). The lesson was learned in one file and not carried to the next, which is a process failure worth naming: a correction to a matching rule should be grepped for across every consumer of the same signal, not applied where it was noticed. **Fix:** resolve the merged PR from the commit via `GET /commits/{sha}/pulls`, and mark done only the issue named in that PR's head branch, `feature/issue-N-*`. The commit message is evidence of what a change *talks about*; the head branch is evidence of what it *is*. **Two gates, both kept:** the branch pattern, and the issue still carrying `agent-ready` — the second keeps this off issues a human worked by hand under a branch that happens to follow the convention (#776, #777 is exactly that case and was correctly left alone in replay). **Replayed against four real merges before shipping:** `f28a1bb` resolves to #771 and never touches #688; `710a30b` resolves to #683; `948057e` resolves to #776 and is gated off by the missing marker; the Dependabot merge `c49d0f3` is not an agent branch and is skipped. **Audit of every `agent-done` issue:** #688 was the only one with no branch; #752, #731, #686, #684, #683 all have real PRs. #688's false label removed by hand; it still needs a human to re-apply `agent-ready`. **The deploy-evidence comment keeps mention-matching** — commenting on an issue a change discusses is useful and harmless; only the label write must be exact. | Process |
| 2026-09-15 | Add a weekly `.trivyignore.yaml` expiry check (#729) that fails 14 days before `expired_at`, and route its failure through the existing `ci-failure-notify.yml` Telegram watcher rather than a new channel | Postmortem action item 4 (`docs/postmortems/2026-09-10-trivy-scan-blocked-staging-61-days.md`): five `libgnutls30` suppressions lapsed silently on `expired_at`, adding a second failure cause to an already-red 61-day pipeline freeze. Hand-rolled the `.trivyignore.yaml` parser in `script/check-trivyignore-expiry.mjs` (no YAML dependency) rather than adding `yaml` as a direct dependency, matching `script/partition-trivy.mjs`'s existing no-dependency convention for CI-only scripts and the file's fixed `id`/`paths`/`statement`/`expired_at` shape | DevOps |
| 2026-09-15 | Record that the agent PAT cannot push any change under `.github/workflows/`, and route around it by pasting the file in a PR comment instead of asking for broader scope (#729) | `git push` was rejected server-side for both a brand-new `.github/workflows/trivyignore-expiry.yml` and a one-line edit to the existing `ci-failure-notify.yml` — GitHub's `workflow`-scope restriction, which no job `permissions:` block (including `GITHUB_TOKEN`) can lift. Unlike #771's issue-write limit, there is no alternate actor to move the write to. Granting the PAT `workflow` scope would fix it but is a credential change (gated list item 3, human-only), so the standing rule is: ship everything pushable, paste the exact workflow file content in a PR comment, label `agent-stuck` | Process |
| 2026-09-16 | `getArtists()`/`getArtworks()` drop `galleryLayout` from their default column selection; only `/api/gallery/hallway` opts back in (#689) | `getArtworks` joined artists with `SELECT *`, so every artwork row carried its artist's full `galleryLayout` JSONB (a width×height cell array) — served unpaginated on `/api/artworks`/`/api/artists`, consumed by home/store/gallery, none of which read that field off an embedded artist. **Chosen:** `getArtists(opts?: { includeGalleryLayout })` selects an explicit column list by default and backfills `galleryLayout: null` on the returned rows; `getArtworks()`'s artist join does the same unconditionally, since no `ArtworkWithArtist` consumer anywhere reads it. `/api/gallery/hallway` — the one caller that regenerates a stale layout — passes `includeGalleryLayout: true` to get the real value. **Scope narrowed on purpose:** the issue also flagged `bio` as droppable "where unused", but `bio` is read directly off `/api/artists` list items (`artists.tsx`) and off `ArtworkWithArtist.artist` (`gallery.tsx`'s detail panel), so it stays. Left the half-dozen other `artists` joins in `storage.ts` (blog posts, exhibitions, related/by-artist artwork lookups) untouched — they return artist-bounded or single-row results, not the unpaginated all-artworks list the issue's HIGH-performance impact was about, and widening the fix to every join site is a separate, unrequested change. Pagination, the issue's other suggestion, is deferred: current data volume doesn't warrant the client/API breaking change it would require, and it was explicitly conditional ("when data volume warrants") rather than the flagged HIGH-severity issue | Architecture |
| 2026-09-16 | The dispatcher gains a 15-minute sweep, inside the same workflow, resolving the issue from a pick step rather than the event payload (#788) | Four `agent-ready` labels applied within 134 seconds on 2026-09-15 produced two runs and two silent cancellations: GitHub holds one running and one pending run per concurrency group, and each new arrival replaces the pending one. The cancelled runs reached no step, so no claim, no comment, and a consumed `labeled` event — the issue shows `agent-ready` and nothing else, the identical state to the three labels applied on 2026-09-12 before the dispatcher existed. #753 named a scheduled sweep as the fallback "if event filtering proves unreliable"; this is that case. **Inside `agent-dispatch.yml`, not beside it**, because a sibling workflow that re-applied the label would make the sender a bot and defeat guard 2; the sweep must invoke the work directly, which means sharing the claim/work/confirm steps. **A pick step resolves the issue** from one of three sources — the labeled event, a `workflow_dispatch` input, or the oldest eligible issue — and every downstream step reads `steps.pick.outputs.issue`, never the event payload, which is empty on schedule and dispatch. **Eligible** is all of: open, `agent-ready` present, no state label, `agent-ready` applied by a `User` (timeline read, the `gated-paths.yml` technique — guard 2 preserved on the sweep path), and **no PR in any state** on `feature/issue-N-*`. That last clause is the one that took thought: after a good run the confirm step releases the claim while the PR is still open, then merged-but-undeployed, and in both windows the issue looks unclaimed. Any-state PR detection keeps the sweep off mid-flight work. **Replayed against the live list before shipping** and it skipped #689 for `agent-working` (a run held it at that moment), #730 for `agent-failed`, and picked #688 — the correct answer, and a different one from the plan's guess an hour earlier, which is what replaying is for. **A bug caught by assertion, not by CI:** the bulk rename of the event reference also rewrote the pick step's own input, so the labeled path would have read its own empty output; an exact-count assertion (one event read, five pick reads) caught it. **Accepted:** a sweep arriving while an event run is pending cancels it — the same mechanism — but now self-healing on the next sweep, worst case 15 minutes. **Oldest = lowest number**, a stated simplification. **Verifiable without a human label**: `gh workflow run` with no input should pick #688 once the current queue drains. | Process |
| 2026-09-16 | Fix for the BEHIND-PR auto-merge stall (#793) is written but could not be shipped in code — pasted in a PR comment for a human to apply | Native `gh pr merge --auto --squash` never updates a PR's branch, and `main`'s `required_status_checks.strict: true` means a PR that falls BEHIND after another agent PR merges underneath it (routine under `concurrency: agent-run`, which serialises back-to-back dispatches into exactly this shape — #791 merging under #792 was the live case) sits forever with every required check green and nothing left to move it. The fix lives entirely inside `.github/workflows/auto-merge.yml`: the existing `pull_request_target` job runs `gh pr update-branch` before arming auto-merge whenever it finds the PR already BEHIND, and a new job triggered on `push: branches: [main]` re-scans every open `agent-review`/`autorelease` PR and updates any that are now BEHIND — the case #793 actually hit, since the second PR was not yet BEHIND at open time, only after the first merged underneath it. **Could not land it**: `auto-merge.yml` is under `.github/workflows/`, which the agent PAT cannot push to at all — confirmed again this run with a direct push attempt, rejected server-side with the same "without `workflow` scope" error as #729, no `permissions:` workaround available (§3 of `specs/AGENT-AUTONOMY-POLICY.md`, already documented; no new rule needed). It is additionally a self-guarded gated path in `script/gated-paths.mjs` (the tier system's own merge condition), so even with push access it would still need `human-approved`. **Shipped instead:** the exact YAML pasted in a PR comment for a human to apply directly, and this row. `specs/workflows/CI-CD.md` §5.3 needs a matching update once the workflow is actually applied — left alone here rather than describing behavior that isn't live yet. | DevOps |
| 2026-09-16 | Move the sweep cron (#788) from `*/15 * * * *` to `7,22,37,52 * * * *`; written but could not be shipped in code — pasted in a PR comment for a human to apply (#795) | The sweep merged at 12:20 UTC (#790) and produced zero `schedule`-event runs across five windows by 13:35 UTC, while `workflow_dispatch` on the same workflow fired immediately and the pick logic itself worked correctly. GitHub's own `schedule` documentation names the start of every 15-minute quarter as a high-load period where queued runs can be delayed or dropped outright, and `*/15` lands on exactly those minutes (`:00 :15 :30 :45`). `doc-agent.yml` (`0 8 * * 1`) and #729's expiry check give no evidence either way for an off-peak minute in this repo, since neither runs at a contended time to compare against. **Chosen:** keep the 15-minute cadence but move it to `7,22,37,52 * * * *` — the documented mitigation, one line, no behavior change beyond which minute fires. **Could not land it**: the change is entirely inside `agent-dispatch.yml`'s `schedule:` block, and that file is under `.github/workflows/`, which the agent PAT cannot push to at all (confirmed again this run with a direct push attempt, same "without `workflow` scope" rejection as #729 and #793 — no `permissions:` workaround exists; no new rule needed, §3 of `specs/AGENT-AUTONOMY-POLICY.md` already covers it). It is also a self-guarded gated path in `script/gated-paths.mjs`, so push access alone would not have been enough — it would still need `human-approved`. **Shipped instead:** the exact diff pasted in a PR comment for a human to apply directly, and this row. The acceptance criteria that need two consecutive live `schedule` runs on the new minute can only be checked after a human applies the diff — flagged in the PR rather than claimed here. | DevOps |
| 2026-09-17 | Every dispatch keeps its execution transcript as a 30-day artifact, linked from the `agent-failed` comment (#789) | #730 ended `is_error: true` after 79 turns with no branch, no PR and no comment; the action hides Claude's output in the job log and discards `claude-execution-output.json` with the runner, so the failure comment's instruction — "re-apply once the cause is understood" — pointed at nothing. A terminal state that cannot be diagnosed forces a human to either re-run blind or work the issue by hand, both of which discard the point of the dispatcher, and failures become rarer and less familiar as the pipeline succeeds. **Chosen:** `actions/upload-artifact` (pinned by SHA, repo convention) after `Work the issue`, `if: always() && steps.claim.outcome == 'success'`, uploading the action's own log; the confirm step reads `steps.transcript.outputs.artifact-url` and puts it in the `agent-failed` comment. **Uploaded on success too**, not only failure: a successful run's transcript is how a human checks *why* the agent scoped #686's blue-green out or chose #683's escape, without re-deriving it from the diff. Same cost, same exposure, no reason to special-case. **`if-no-files-found: ignore`** because a sweep that picks nothing skips the work step and writes no transcript — not a failure. **Exposure decided explicitly:** the repository is public (#545 open), so the artifact is downloadable by anyone with read access. Repository contents are already public; secrets are environment rather than files; but the transcript is the action's own record and is not run through the job-log masker, so the first real artifact is to be grepped for both token prefixes and the result recorded on #789 before this is called safe — the one branch of the plan that cannot be settled before a run. Retention is 30 days, set explicitly rather than the 90-day default: long enough to diagnose a failure the following working week, short enough that a public transcript is not a permanent record. **A bash quirk caught by `bash -n`, not by CI:** an apostrophe inside a `${VAR:-default}` word within a double-quoted string is parsed as an opening quote; the default text was reworded. CI never executes this file, so that check is the only one that would have caught it before a live run. | Process |
| 2026-09-16 | `script/check-deploy-freshness.mjs` measures environment drift by the age of the oldest commit an environment is missing, not the age of the deployed SHA itself; default threshold 3 days, checked daily; the workflow to run it could not be shipped in code (#730) | Postmortem action item 6 (`docs/postmortems/2026-09-10-trivy-scan-blocked-staging-61-days.md`): `/api/version` reports the CHANGELOG-derived release string and read identically (`v3.19.0`) on a freshly-deployed staging and a 67-day-stale production two months apart — it cannot be the freshness signal. **Chosen metric:** commits on `main` the deployed SHA does not contain (`git log <sha>..main`), keyed on the oldest one's date. A deploy one commit behind a `main` that hasn't moved in weeks reads as fresh; a deploy that missed a commit an hour ago reads as fresh until that commit ages past the threshold — this is what actually hurt in the incident, where staging sat on one fixed SHA while `main` moved underneath it for 61 days. **3-day default, not 1:** the issue's success criterion ("fires within one day") is about detection latency, satisfied by the daily cron regardless of threshold, not about the threshold value itself; 3 days tolerates a quiet weekend without paging on every Monday. **Both environments' actual deployed SHA come from `IMAGE_TAG` in each `.env`** (staging via ci.yml's deploy job, production via deploy-production.yml) — read over SSH with the same `DEPLOY_HOST`/`DEPLOY_SSH_KEY` secrets the existing deploy workflows use, not from `/health`'s build-number fallback, since SSH access already exists and needs no additional resolution step. **Could not land the workflow**: a new `.github/workflows/deploy-freshness.yml` and a one-line trigger-list edit to `ci-failure-notify.yml` are both blocked by the same PAT `workflow`-scope restriction as #729 and #793 (§3 of `specs/AGENT-AUTONOMY-POLICY.md`, already documented — no new rule needed). **Shipped instead:** the script plus its test suite (a throwaway git repo per test, not this checkout, since the CI job that would run these tests checks out with the default shallow `fetch-depth: 1`), and the exact YAML pasted in a PR comment for a human to apply. `specs/workflows/CI-CD.md` needs a matching section once the workflow is actually applied — left alone here rather than describing behavior that isn't live yet, matching the #793 precedent. | DevOps |
| 2026-09-17 | Transcript artifacts are redacted before upload, and the upload refuses if token material survives; the first artifact leaked `RELEASE_PAT` and was deleted (#789 incident) | #799 began keeping every dispatch's `claude-execution-output.json` as a 30-day artifact, with an explicit exposure decision that said the first real artifact would be grepped for token prefixes *before* being declared safe. The first real artifact — #800's run, uploaded 09:00 UTC 2026-09-17 — **failed that check**: `RELEASE_PAT` appeared in clear four times. Cause: `anthropics/claude-code-action` configures the git remote as `https://x-access-token:<PAT>@github.com/…`, the agent ran `git remote -v` while orienting itself, and the transcript is the action's raw record — unlike the job log, it is not passed through GitHub's secret masker. The artifact sat on a public repository for roughly an hour, downloadable by anyone with read access. **Response, in order:** the artifact was deleted (the only `agent-run-*` artifact that existed); the local copy used for the grep was removed; the token is being rotated by the developer (a credential action, policy §1 item 3 — already planned that morning to add `workflow` scope, now mandatory); and this row plus a redaction step. **The redaction step** runs between `Work the issue` and the upload, masks the remote-URL credential form (`x-access-token:…@` → `x-access-token:***@`) and every known token prefix (`github_pat_`, `ghp_`/`gho_`/`ghs_`/`ghu_`/`ghr_`, `sk-ant-`) in place, and then **greps for survivors and refuses to upload — deleting the file — if any remain**, on the principle that an upload which might leak is worse than no upload. Tested against synthetic lines in all three forms before shipping: zero survivors. **What the exposure decision got wrong:** it reasoned that secrets are "environment, not files" and would not appear in the transcript. That was true of the secret *values as stored* and false of the transcript, which records command *output* — and the action itself writes the token into git configuration that ordinary commands print. The check the plan required is what caught it; the plan's confidence about the outcome was unearned. **Not done, deliberately:** no attempt to scrub the token from GitHub's own run logs (they are masked there) or from the agent's session history on the runner (ephemeral, gone). | Security |
| 2026-09-17 | Route `agent-stuck` and `agent-no-change` through the existing Telegram watcher, and clear the opposite mutually-exclusive label when mirroring either; written but could not be shipped in code — pasted in a PR comment for a human to apply (#800) | PR #796 sat `agent-stuck` for ~17 hours, found only by accident: `AGENT-PIPELINE.md` §3 had `agent-stuck` "wait silently and page nobody" by design, correct while a human read every PR, wrong once #744 put the developer out of the loop by default. Four of the last five agent runs on devops issues ended `agent-stuck` (#729, #793, #795, #730) for the identical root cause (§3's `.github/workflows/` PAT restriction), and only the ones a human happened to be watching were resolved promptly. Separately, #730 was found carrying both `agent-failed` (from a 2026-09-15 run) and `agent-stuck` (from a 2026-09-16 run) at once — the confirm step's mirror adds one state and drops `agent-working` but never clears the other, even though `AGENT-PIPELINE.md` already declares the two mutually exclusive. **Fix, entirely inside `agent-dispatch.yml`'s "Confirm the run produced work" step:** reuse `ci-failure-notify.yml`'s Telegram path (#631) rather than a new channel — one `curl` to the same bot/chat, gated on `TELEGRAM_BOT_TOKEN` being set, wrapped in a `notify_telegram()` helper so the two call sites (the `no-change-needed` branch and the `agent-stuck` mirror) don't duplicate the request. Fires once, not repeatedly: the step runs once per dispatch, and writing a state label removes the issue from the sweep's eligible set (the `pick` step already excludes `agent-stuck`/`agent-no-change`/`agent-failed`), so there is no second run to double-post. The `agent-stuck` message includes the first line of the last PR comment — the escalation AGENT-PIPELINE.md §3 already mandates be posted before the label is applied — rather than inventing a second place for the agent to write a summary. Best-effort (`|| true` on the `curl`): a Telegram outage must not fail the step that releases the `agent-working` claim. Label-clearing: writing `agent-stuck` also removes `agent-failed` and vice versa, at both sites that can apply either (the no-PR branch and the PR-signal-mirror branch), with `|| true` since removing an absent label 404s. **Not done:** making `agent-stuck` page repeatedly, or treating it as failure — the issue explicitly rules this out, and the state's meaning (work is sound and resumable) stays correct; only its silence was the defect. **Could not land it:** the entire fix lives inside `.github/workflows/agent-dispatch.yml`, which the agent PAT cannot push to at all — confirmed again this run with a direct push attempt on this branch, rejected server-side with the same "without `workflow` scope" error as #729/#793/#795, no `permissions:` workaround available; no new rule needed, §3 of `specs/AGENT-AUTONOMY-POLICY.md` already covers it. It is additionally a self-guarded gated path in `script/gated-paths.mjs`, so push access alone would not have been enough — it would still need `human-approved`. **Shipped instead:** the exact diff pasted in a PR comment for a human to apply directly, plus this row. `AGENT-PIPELINE.md` §3's "pages nobody" sentence, and the acceptance criteria that need a live demonstrated run, are left unclaimed here rather than described as true before a human applies the diff — same convention as #793's and #795's rows. | DevOps |
| 2026-09-17 | Upgrade `eslint-plugin-react-hooks` 5.2.0 → 7.1.1 and fix or suppress every anti-pattern the new React-Compiler-style rules found (#608), rather than pinning or reverting | v7's rules are stricter and behavior-driven (ref writes during render, `setState` called directly inside an effect, canvas/Three.js calls the compiler can't prove are pure), not merely a lint-style bump — Dependabot #524 was closed for exactly this reason (21 findings, not auto-mergeable). **Classification used throughout:** (1) a ref written during the render body (`selectedArtworkRef.current = x`, `hallway-gallery-3d.tsx`, `maze-gallery-3d.tsx`) moved into a dedicated `useEffect` — the standard "latest ref" fix; (2) editable local state seeded from a prop/query/URL value on change (`enquiry-dialog.tsx`, `artist-dashboard.tsx`, `curator-dashboard.tsx` ×2, `store.tsx`) converted to the React-documented "adjusting state during render" pattern (compare against a tracked previous value, `setState` directly in the render body) instead of a `useEffect`, which both satisfies the rule and drops the extra commit the effect cost; (3) one-time feature detection (`use-mobile.tsx`'s viewport query, `share-buttons.tsx`'s `navigator.share` check) moved to `useSyncExternalStore` / a lazy `useState` initializer rather than an effect; (4) an impure `Math.random()` call inside `useMemo` (`sidebar.tsx`) moved to a lazy `useState` initializer, and a `window.location.href` redirect written directly in the render body (`set-password.tsx`) moved into an effect — both genuine bugs the rule caught correctly; (5) three call sites judged **false positives** or **deliberate, hard-to-restructure patterns** and suppressed with `eslint-disable-next-line` plus a `Why:` comment rather than refactored: `hallway-gallery-3d.tsx`'s `buildScene` (the compiler's escape analysis can't see through Canvas2D/Three.js calls and conservatively flags a prop-derived string as mutated, though it's only ever read — confirmed against a live render, see below), `carousel.tsx`'s embla `onSelect` priming call (no snapshot API to move to `useSyncExternalStore` without a wrapper around the whole embla instance), and both files' `setWebglError` calls inside their large setup/teardown effects (WebGL capability can only be known by attempting a context, and splitting just that check out of the surrounding effect would itself be the riskier change). **`createArtistPoster` was also reordered** (declared before its call site in `hallway-gallery-3d.tsx`) to fix a genuine "used before declared" compiler error — a text-only move, hoisting already made it work at runtime. **Verified beyond lint/build/tests:** `docker compose up` locally with a seeded artist and exhibition-ready artwork, driven through both 3D components (`/gallery` hallway and `/artists/<slug>` maze) with Playwright — WASD movement, the hanging sign text (the exact code path carrying the suppression), and the minimap all rendered correctly, with zero console/page errors beyond an expected 401 (no session) and a CSP block on the test artwork's external Unsplash URL (a seed-data artifact, not a code path this PR touches). | Architecture |
